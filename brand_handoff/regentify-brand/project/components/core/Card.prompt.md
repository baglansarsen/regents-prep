Soft white surface card with the brand shadow. Use `selected` for the 2px green chosen state, `interactive` for tappable cards (hover lift).

```jsx
<Card interactive onClick={open}><h3>Chemistry</h3></Card>
<Card selected>…</Card>
```