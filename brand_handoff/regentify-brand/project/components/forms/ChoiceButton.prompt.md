A multiple-choice answer row for quizzes. Render in `idle` while answering, then switch each to `correct`/`wrong`/`dim` to reveal the result.

```jsx
<ChoiceButton label="A" state="correct">A trinomial — three terms</ChoiceButton>
```

States: idle, correct (green), wrong (coral), dim (faded non-answers).