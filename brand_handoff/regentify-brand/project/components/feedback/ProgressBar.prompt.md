Mint track + green-gradient fill. The everyday progress meter.

```jsx
<ProgressBar value={68} label="Algebra I" count="12 / 18" />
```

Set `showMeta={false}` for a bare bar; `height` controls thickness.