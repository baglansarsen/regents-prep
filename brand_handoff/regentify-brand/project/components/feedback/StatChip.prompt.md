A gamification pill. Each metric owns one accent color — don't recolor them.

```jsx
<StatChip kind="xp" value="1,250 XP" />
<StatChip kind="streak" value="7-day streak" />
```

Kinds: xp (🪙), streak (🔥), lives (❤️), pro (⭐), info (ℹ️). `solid` gives a soft tinted bg.