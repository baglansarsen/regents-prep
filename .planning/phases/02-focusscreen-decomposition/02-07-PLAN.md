---
phase: 02-focusscreen-decomposition
plan: 07
type: execute
wave: 5
depends_on: ["02-06"]
files_modified:
  - mobile/src/components/FocusScreen/FocusSetupScreen.jsx
  - mobile/src/components/FocusScreen/TaskInput.jsx
  - mobile/src/components/FocusScreen/TaskList.jsx
  - mobile/src/components/FocusScreen/SoundPicker.jsx
  - mobile/src/components/FocusScreen/BackgroundPicker.jsx
  - mobile/src/components/FocusScreen/__tests__/TaskInput.test.jsx
  - mobile/src/components/FocusScreen/__tests__/TaskList.test.jsx
  - mobile/src/components/FocusScreen/__tests__/SoundPicker.test.jsx
  - mobile/src/components/FocusScreen/__tests__/BackgroundPicker.test.jsx
autonomous: true
requirements: [FOCUS-03, FOCUS-05]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All four components fire callback props, so per **D-01** each gets an interaction test; per **D-02** these are the thinnest prop-forwarding wrappers in the phase and therefore the likeliest place for a wrong-handler mis-wire that hook tests cannot see"
    - "`TaskList` renders zero rows and no placeholder copy for an empty array, renders one row for one task and N rows for N with no singular/plural copy difference, and preserves the array's order — 02-UI-SPEC.md UI Considerations `empty` and `zero-one-many` rows, and the FOCUS-05 empty and ordering probes"
    - "`TaskList` rows only ever toggle: the trailing control on a setup row toggles the task's done state, it does not remove it. No delete capability exists anywhere in this screen today and none is added (02-PATTERNS.md confirms both call sites toggle)"
    - "`TaskInput` shows its add affordance only when the field holds non-blank text and hides it when the field is blank or whitespace-only, exactly as the container did"
    - "`TaskInput` keeps the field single-line with no explicit truncation — long text scrolls within the field, which is the existing accepted behavior (02-UI-SPEC.md UI Considerations, `long-text` row) — and forwards raw text without trimming, because the trim lives in the domain hook's add function"
    - "`SoundPicker` decides active state by id against the currently selected sound, and `BackgroundPicker` by id against the currently selected scene; both render options in the order of the list they are given (FOCUS-05 adjacency and ordering probes)"
    - "`BackgroundPicker` draws each swatch in that scene's own bottom tone and marks the selected one with the thicker text-colored border — the selection indicator is a border change, not a background change"
    - "`FocusSetupScreen` renders the same tree after this plan as before it, and after this plan it holds no option-section JSX at all"
    - "`npm --prefix mobile run check` exits 0"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. A discrepancy found during the move is recorded in the SUMMARY as a finding and left as-is — PROJECT.md is explicit that a behavior change discovered as 'needed' is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT add a delete-task capability. The trailing control on a setup task row toggles done state today; turning it into a delete because the glyph looks like one would be a silent, destructive behavior change."
      category: safety
    - statement: "MUST NOT add empty-state copy, a placeholder row, or a count line to the task list. 02-UI-SPEC.md records that zero tasks renders nothing today and that this is accepted, not a gap to fill."
      category: values
    - statement: "MUST NOT move the trim-before-use of task text out of the domain hook and into the input component. Where the trim happens is behavior, not formatting, and this phase preserves it at its current site (02-RESEARCH.md Security Domain V5)."
      category: safety
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or `.todo`-ing any existing test, and MUST NOT ship a vacuous new one. A render test that only proves no exception was thrown certifies nothing while looking like proof."
      category: transparency
    - statement: "MUST NOT substitute the shared button, card, shadow, pill, or section-label factories for this screen's own local flat styles, and MUST NOT round the screen's off-grid spacing values to a tidier scale. Both are visual changes disguised as cleanup (02-UI-SPEC.md Component Inventory and Spacing Scale)."
      category: values
  artifacts:
    - path: "mobile/src/components/FocusScreen/TaskInput.jsx"
      provides: "Task entry row with its conditional add affordance (FOCUS-03)"
      contains: "Add a task..."
      min_lines: 35
    - path: "mobile/src/components/FocusScreen/TaskList.jsx"
      provides: "Setup-phase task rows with toggle control (FOCUS-03)"
      contains: "onToggle"
      min_lines: 25
    - path: "mobile/src/components/FocusScreen/SoundPicker.jsx"
      provides: "Background-sound chip row (FOCUS-03)"
      contains: "Background sound"
      min_lines: 30
    - path: "mobile/src/components/FocusScreen/BackgroundPicker.jsx"
      provides: "Scene swatch row (FOCUS-03)"
      contains: "Background"
      min_lines: 30
    - path: "mobile/src/components/FocusScreen/__tests__/TaskList.test.jsx"
      provides: "Interaction coverage per D-01, plus the empty and ordering edges"
      contains: "toHaveBeenCalledWith"
      min_lines: 30
  key_links:
    - from: "mobile/src/components/FocusScreen/FocusSetupScreen.jsx"
      to: "mobile/src/components/FocusScreen/TaskInput.jsx"
      via: "the container forwards the current field value, the value setter, and the add handler from the orchestration hook"
      pattern: "TaskInput"
    - from: "mobile/src/components/FocusScreen/BackgroundPicker.jsx"
      to: "mobile/src/hooks/useFocusScreenState.js"
      via: "the scene list is the array exported by the orchestration hook module and passed down as a prop"
      pattern: "backgrounds"
---

<objective>
Split the last four sections out of the setup container — task entry, task rows, background sound, and scene — completing FOCUS-03's decomposition of FocusScreen's UI into named sub-components.

**As a** student adding tasks and picking an ambience before starting, **I want to** every field, row, chip, and swatch to behave identically, **so that** the last of the refactor lands as invisibly as the rest of it.

Purpose: after this plan there is no option-section JSX left anywhere in a container — every piece of FocusScreen's UI is a named, individually tested component, which is the template Phases 3-5 copy.

Output: four components under `components/FocusScreen/`, four test files, and a setup container reduced to chrome plus composition.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md
@.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md
@.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md
@.planning/phases/02-focusscreen-decomposition/02-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See plan 02-01's identically named section for the full Phase 2 list. This plan contributes:

| Symbol / file | Kind | Path |
|---|---|---|
| `TaskInput` | default export, component | `mobile/src/components/FocusScreen/TaskInput.jsx` |
| `TaskList` | default export, component | `mobile/src/components/FocusScreen/TaskList.jsx` |
| `SoundPicker` | default export, component | `mobile/src/components/FocusScreen/SoundPicker.jsx` |
| `BackgroundPicker` | default export, component | `mobile/src/components/FocusScreen/BackgroundPicker.jsx` |
| Four matching `*.test.jsx` files | test files | `mobile/src/components/FocusScreen/__tests__/` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract TaskInput and TaskList</name>

  <files>mobile/src/components/FocusScreen/TaskInput.jsx, mobile/src/components/FocusScreen/TaskList.jsx, mobile/src/components/FocusScreen/__tests__/TaskInput.test.jsx, mobile/src/components/FocusScreen/__tests__/TaskList.test.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` as plans 02-04 and 02-06 left it — the tasks section: its label with the parenthetical, the bordered entry row with its field and conditional add affordance, and the row map below it. Plus the task-input-row, task-input-field, add-button, add-button-text, row, checkbox, and text style keys.
    - `mobile/src/hooks/useFocusSession.js` lines 68-78 — where task text is trimmed and where a task record's shape is defined. The trim stays there.
    - `mobile/src/components/FocusScreen/ActiveTaskList.jsx` as plan 02-05 wrote it — the sibling list component for the active phase. These two are deliberately separate components, not one with a variant flag (02-RESEARCH.md Open Question 1's own recommendation), because each then has a single obvious callback prop under D-01.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` section "UI Considerations" — the `empty`, `zero-one-many`, and `long-text` rows this pair pins.
  </read_first>

  <behavior>
    `TaskInput.test.jsx`:
    - The section label with its parenthetical and the field's placeholder render.
    - Typing calls the value setter with the typed text — assert the argument.
    - Submitting the field calls the add handler exactly once.
    - With a blank value the add affordance is absent; with a whitespace-only value it is still absent;
      with non-blank text it renders and pressing it calls the add handler exactly once.
    - The field is single-line: it carries no multiline flag and no line limit, so long text scrolls
      within it rather than wrapping or being cut — the `long-text` UI Consideration, preserved.

    `TaskList.test.jsx`:
    - With three tasks, all three texts render in the order of the array given.
    - Pressing a row's trailing control calls the toggle handler with that row's id — assert the argument.
    - The trailing control is a toggle, not a delete: no remove handler prop exists on this component at
      all, so there is nothing that could silently become one.
    - With one task, exactly one row renders; with an empty array, no row renders and no placeholder copy
      appears. Assert the absence of any text node in the list region, not merely the absence of a string.
  </behavior>

  <action>
    Create both components as default exports under `mobile/src/components/FocusScreen/`.

    `TaskInput.jsx`: move the tasks label and the entry row across unchanged — the label with its inline
    lighter-weight parenthetical, the bordered row on the muted surface, the field with its placeholder,
    placeholder color, value, change handler, submit handler, and return-key type, and the add affordance
    rendered only when the value has non-blank content. Keep the non-blank condition exactly as written;
    it is a trimmed-length check on the display value, which is not the same thing as trimming the value
    that gets stored. Keep the row's off-grid horizontal and vertical paddings and the add affordance's
    left padding. Props: the current value, a value setter, and an add handler. Forward raw text; do not
    trim on the way out.

    `TaskList.jsx`: move the setup row map across unchanged — each task as a row with its gap and vertical
    padding, an unfilled checkbox carrying only the border color, the task text, and a trailing control
    with its hit slop whose press toggles that task. Note that the trailing control's glyph reads like a
    dismissal but its handler is the toggle; preserve the handler, not the appearance's implication.
    Props: the task array and a toggle handler. No remove prop.

    Both read the theme inside and keep local theme-parameterized style factories with only their own
    keys, copied byte-for-byte. Neither imports the button, card, shadow, pill, or section-label factories
    from `../../styles/duo`.

    Write both test files. Mock `'../../../context/ThemeContext'` in each. Await every `render()`. Use
    `fireEvent.changeText`, `fireEvent(node, 'submitEditing')`, and `fireEvent.press`. Assert arguments
    with `toHaveBeenCalledWith` where a handler takes one.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen/__tests__/TaskInput.test.jsx src/components/FocusScreen/__tests__/TaskList.test.jsx 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 2 test suites, meaning one of the two paths did not resolve.</fails_when>
    <automated>grep -cEi 'delete|remove' mobile/src/components/FocusScreen/TaskList.jsx</automated>
    <fails_when>the printed count is greater than 0 — a delete or remove affordance appeared in a list that has only ever toggled, which is a destructive behavior change.</fails_when>
    <automated>grep -c 'trim()' mobile/src/components/FocusScreen/TaskInput.jsx</automated>
    <fails_when>the printed count is greater than 1 — more than the single existing display-side non-blank check is present, meaning storage-side trimming was relocated into the component.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both components exist with default exports and local theme-parameterized style factories.
    - `TaskInput.jsx` contains the placeholder `Add a task...` and the add label `Add` byte-for-byte.
    - `TaskList.jsx`'s props are the task array and a single toggle handler — no remove or delete prop.
    - `TaskInput.test.jsx` asserts the add affordance is absent for blank AND whitespace-only values.
    - `TaskList.test.jsx` asserts row order, the toggle argument, and that an empty array renders no text node.
    - `npm --prefix mobile test -- src/components/FocusScreen/__tests__/TaskInput.test.jsx src/components/FocusScreen/__tests__/TaskList.test.jsx` passes.
  </acceptance_criteria>

  <done>Task entry and task rows are named components with interaction tests pinning the empty, ordering, blank-field, and toggle-not-delete behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract SoundPicker and BackgroundPicker</name>

  <files>mobile/src/components/FocusScreen/SoundPicker.jsx, mobile/src/components/FocusScreen/BackgroundPicker.jsx, mobile/src/components/FocusScreen/__tests__/SoundPicker.test.jsx, mobile/src/components/FocusScreen/__tests__/BackgroundPicker.test.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` in its current state — the sound section and the scene section, plus the chip-row, chip, chip-emoji, chip-text, swatch-row, and swatch style keys.
    - `mobile/src/hooks/useFocusSession.js` lines 28-34 — the real sound option records.
    - `mobile/src/hooks/useFocusScreenState.js` as plan 02-01 wrote it — the exported scene array with each scene's id, glyph, label, two tone values, and accent.
    - `mobile/src/components/FocusScreen/SubjectPicker.jsx` as plan 02-06 wrote it — the sound chips use the identical chip markup and style keys; mirror that shape rather than inventing a second one.
  </read_first>

  <behavior>
    `SoundPicker.test.jsx`:
    - The section label and every option's label render, in the order of the list given.
    - The option whose id matches the current selection renders in its active treatment; the others do not.
    - Pressing an option calls the select handler with that option's whole record — assert the argument.
    - The off option is a normal option: it renders and selects like any other, with no special casing.

    `BackgroundPicker.test.jsx`:
    - The section label and all six scene glyphs render, in the order of the list given.
    - Pressing a swatch calls the select handler with that scene's whole record — assert the argument.
    - The swatch whose id matches the current scene carries the thicker selection border and the others
      carry the transparent default; assert on the resolved style, not on a class name.
  </behavior>

  <action>
    Create both components as default exports under `mobile/src/components/FocusScreen/`.

    `SoundPicker.jsx`: move the sound section across unchanged — the section label, the wrapping chip row,
    and the option map where each chip carries its glyph and label and switches to the filled brand
    treatment when its id matches the current selection. This is the same chip markup `SubjectPicker`
    uses; keep the two components separate rather than generalizing a shared chip row — generalizing is a
    design change, and this phase only moves code. Props: the option list, the currently selected sound,
    and a select callback.

    `BackgroundPicker.jsx`: move the scene section across unchanged — the section label, the wrapping row
    with its gap, and the swatch map where each swatch is sized square with its radius, filled with that
    scene's own bottom tone, carrying its glyph, and taking the thicker text-colored border only when its
    id matches the current scene. Keep the swatch's off-grid dimensions and both border widths exactly —
    the default transparent border and the thicker selected one are different widths and the difference is
    the selection indicator. Props: the scene list, the currently selected scene, and a select callback.

    Both read the theme inside and keep local theme-parameterized style factories with only their own
    keys, copied byte-for-byte. Neither imports the button, card, shadow, pill, or section-label factories
    from `../../styles/duo`.

    Write both test files. Mock `'../../../context/ThemeContext'` in each. Source the sound records from
    the real exported array in `mobile/src/hooks/useFocusSession.js` and the scene records from the
    exported array in `mobile/src/hooks/useFocusScreenState.js` rather than inventing fixtures. Await
    every `render()`. Use `fireEvent.press` and assert arguments with `toHaveBeenCalledWith`.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen/__tests__/SoundPicker.test.jsx src/components/FocusScreen/__tests__/BackgroundPicker.test.jsx 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 2 test suites, meaning one of the two paths did not resolve.</fails_when>
    <automated>grep -c -e 'duoBtn' -e 'cardShadow' -e 'elevatedCard' -e 'pillTab' mobile/src/components/FocusScreen/SoundPicker.jsx mobile/src/components/FocusScreen/BackgroundPicker.jsx</automated>
    <fails_when>either printed count is greater than 0 — a shared style factory this screen has never used was pulled in, which is a visual change.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both components exist with default exports and local theme-parameterized style factories.
    - `SoundPicker.jsx` contains the section label `Background sound`; `BackgroundPicker.jsx` contains the section label `Background`.
    - Both tests source their fixtures from the real exported arrays, not from literals invented in the test.
    - `BackgroundPicker.test.jsx` asserts the selected swatch's border differs from an unselected one.
    - `npm --prefix mobile test -- src/components/FocusScreen/__tests__/SoundPicker.test.jsx src/components/FocusScreen/__tests__/BackgroundPicker.test.jsx` passes.
  </acceptance_criteria>

  <done>The sound and scene sections are named pickers with interaction tests pinning their active-state comparisons and handler arguments.</done>
</task>

<task type="auto">
  <name>Task 3: Recompose the remaining sections of FocusSetupScreen</name>

  <files>mobile/src/components/FocusScreen/FocusSetupScreen.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` in its current state — you are removing four sections and adding four renders.
    - `mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx` as plan 02-04 wrote it — it must keep passing unchanged; if it needs editing to stay green, the recomposition changed behavior.
    - The four components from Tasks 1 and 2 — their exact prop names.
  </read_first>

  <action>
    Replace the inline tasks section, sound section, and scene section with single renders of the four
    components, forwarding the lists, current values, and handlers the container already receives from the
    orchestration hook. The task entry and the task rows are two separate renders, one after the other, in
    the same order the inline version had them.

    Delete from this file every style key only the four moved sections used: the chip row, chip, chip
    glyph, chip text, task input row, task input field, add button, add button text, row, checkbox, text,
    swatch row, and swatch keys. Also delete the section-label key now that no section label is rendered
    by this container any more — each extracted component carries its own copy.

    What remains in this container: the safe-area wrapper, the keyboard-avoiding wrapper, the scroll
    container, the header row with its title, history link and close control, the four section renders
    plus the three from plan 02-06, the start action, the conditional past-sessions summary link, and the
    bottom spacer — plus the style keys for exactly those.

    Do NOT edit `FocusSetupScreen.test.jsx`. It was written against rendered output, and rendered output
    is what must be unchanged. If it fails, the recomposition is wrong — fix the component, not the test.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 18 test suites, meaning a file in the directory was not discovered.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>D=$(git diff --stat -- mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx) || exit 1; test -z "$D" && echo UNCHANGED || { echo "MODIFIED:"; echo "$D"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `UNCHANGED` — the container's existing test was edited, meaning rendered output changed and parity was not preserved.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` renders `TaskInput`, `TaskList`, `SoundPicker`, and `BackgroundPicker` and holds none of their JSX inline.
    - `grep -c -e 'sectionLabel' -e 'todoInputRow' -e 'bgSwatch' -e 'chipEmoji' mobile/src/components/FocusScreen/FocusSetupScreen.jsx` prints 0.
    - `mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx` is unmodified and still passes.
    - `npm --prefix mobile test -- src/components/FocusScreen` passes with all 18 suites discovered.
    - `npm --prefix mobile run check` exits 0.
  </acceptance_criteria>

  <done>No option-section JSX remains inline in any container; every piece of FocusScreen's UI is a named, individually tested component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client text entry → session state | The task field is user-input surface that already existed; this plan relocates it into `TaskInput` without changing how its value is read, trimmed, or stored. The trim stays in the domain hook's add function. No new boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Tampering | `TaskList` trailing control | low | mitigate | The trailing glyph reads like a dismissal but its handler toggles. Turning it into a delete during the move would silently destroy user-entered tasks with no undo. Mitigated by the no-remove-prop design, the prohibition above, and the delete/remove grep in Task 1's verify block. |
| T-02-14 | Tampering | task text field in `TaskInput` | low | mitigate | ASVS V5: the existing trim-before-use must not migrate into the component. Enforced by the trim-count grep in Task 1's verify block; the hook test from plan 02-01 covers the trim behavior itself. |
| T-02-SC | Tampering | package-manager installs | low | accept | No install task exists in this plan. `02-RESEARCH.md` records the Package Legitimacy Audit as not applicable — zero new external packages, no `[ASSUMED]`/`[SUS]` entries, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0.
- `npm --prefix mobile test -- src/components/FocusScreen` passes with all 18 suites discovered.
- `mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx` is byte-identical to the version plan 02-04 committed.
- `git status --porcelain chromebook src` is empty — mobile-only change set.
</verification>

<success_criteria>
- FOCUS-03 fully satisfied: every part of FocusScreen's UI lives in a named sub-component under `components/FocusScreen/`.
- FOCUS-05 advanced: four more interaction-tested components per D-01, each asserting handler arguments.
- The FOCUS-05 `empty` and `ordering` edges and the UI-SPEC `empty`, `zero-one-many`, and `long-text` UI Considerations are all pinned by automated assertions.
</success_criteria>

<output>
Create `.planning/phases/02-focusscreen-decomposition/02-07-SUMMARY.md` when done. Record: the final prop lists of the four components, confirmation that `FocusSetupScreen.test.jsx` passed unedited, the final line counts of all three containers, and any behavior discrepancy noticed during the move — as a finding, not a fix.
</output>
