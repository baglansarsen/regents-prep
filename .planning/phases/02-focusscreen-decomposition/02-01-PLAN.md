---
phase: 02-focusscreen-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md
  - mobile/src/hooks/useFocusScreenState.js
  - mobile/src/screens/FocusScreen.jsx
  - mobile/src/components/FocusScreen/FocusDoneScreen.jsx
  - mobile/src/__tests__/useFocusScreenState.test.js
  - mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx
autonomous: true
requirements: [FOCUS-01, FOCUS-02, FOCUS-03, FOCUS-04, FOCUS-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` exists and is committed BEFORE any line of `mobile/src/screens/FocusScreen.jsx` changes — the parity baseline for FOCUS-06 is worthless if written after the code moved"
    - "The characterization notes state explicitly, in their own words, that no search input, no filter control, and no category concept exists anywhere in `FocusScreen.jsx` or `useFocusSession.js` — FOCUS-01's literal wording was inherited from a stale line in `.planning/codebase/CONCERNS.md` and must be recorded as a requirement-text inaccuracy, never invented into code (RESEARCH.md Pitfall 1)"
    - "`mobile/src/screens/FocusScreen.jsx` calls exactly one hook — `useFocusScreenState(navigation)` — and reads no React context directly (D-02 relies on the screen being a pure renderer for the interaction tests to mean anything)"
    - "`useFocusScreenState` calls `useFocusSession(uid, earnRP, handlePomodoroComplete)` internally and spreads its return value; `mobile/src/hooks/useFocusSession.js` is byte-identical before and after this plan"
    - "A fresh mount of `useFocusScreenState` returns `phase === 'idle'`, `todos` as an empty array, `buddyMessage === null`, `goalCelebModal === false`, `isActive === false`, `isDone === false`, and `background` equal to the first entry of `BACKGROUNDS` — the first-mount/empty edge (FOCUS-04 empty probe)"
    - "`handleAddTodo()` with a whitespace-only value is a no-op that adds no row, and `handleCustomSubject()` with a whitespace-only value leaves `subject` unchanged — the existing `.trim()`-before-use behavior is preserved at exactly the sites it happens today, not relocated (RESEARCH.md Security Domain V5)"
    - "`toggleTodo(id)` flips only the matching row's `done` field and leaves array order unchanged — the todos-ordering edge (FOCUS-04 ordering probe)"
    - "With `sessionGoal === 0`, `goalCelebModal` stays `false` no matter how the session advances — the zero side of the goal-celebration boundary (FOCUS-04 adjacency probe)"
    - statement: "When `pomodoroCount` reaches exactly `sessionGoal` (equality, not >), the celebration fires once and `goalCelebModal` becomes true after the existing 600 ms delay — the equality side of the goal-celebration adjacency boundary"
      verification: backstop
    - "The two end-session confirmation paths are preserved as two DISTINCT behaviors: the back-gesture guard finishes with `navigation.dispatch(e.data.action)` and the active-header close button finishes with `navigation.goBack()`. They share only the dialog copy. RESEARCH.md Open Question 2 called these byte-identical; re-reading `mobile/src/screens/FocusScreen.jsx` lines 186-203 and 356-373 this session shows they are not, so a blind dedupe into one callback would be a behavior change"
    - "`mobile/src/components/FocusScreen/FocusDoneScreen.jsx` renders the done-phase tree that `mobile/src/screens/FocusScreen.jsx` rendered inline before this plan, with every copy string, style key, and numeric value carried over unchanged (UI-SPEC parity lock)"
    - "`npm --prefix mobile run check` exits 0 — full jest suite plus the babel parse-check of changed src files"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. If a bug, inconsistency, or 'obvious improvement' surfaces during the move, it is recorded in the plan SUMMARY as a finding and left exactly as it is — PROJECT.md is explicit that a behavior change discovered as 'needed' is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT write or amend `02-CHARACTERIZATION.md` after the extraction has begun, and MUST NOT adjust it to match what the new code does. Its only value is as an independent pre-extraction baseline; a retro-fitted baseline turns FOCUS-06 into a tautology."
      category: transparency
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or `.todo`-ing any existing test, and MUST NOT ship a vacuous new one. A render test that only proves no exception was thrown certifies nothing while looking like proof."
      category: transparency
    - statement: "MUST NOT slip opportunistic work into this structural refactor — no new feature, no UX fix, no dependency change, no TypeScript, no state-management library, no edits outside `mobile/`."
      category: values
  artifacts:
    - path: ".planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md"
      provides: "Pre-extraction behavior baseline for FOCUS-01, and the reference the FOCUS-06 manual parity pass is scored against"
      contains: "Selection state machines"
      min_lines: 60
    - path: "mobile/src/hooks/useFocusScreenState.js"
      provides: "The single orchestration hook FocusScreen calls (FOCUS-02); wraps useFocusSession and owns the screen's former local state, effects, and handlers"
      contains: "useFocusSession"
      min_lines: 120
    - path: "mobile/src/screens/FocusScreen.jsx"
      provides: "Screen reduced toward a pure renderer: one hook call, branch on returned phase flags"
      contains: "useFocusScreenState"
    - path: "mobile/src/components/FocusScreen/FocusDoneScreen.jsx"
      provides: "Done-phase container component (FOCUS-03), first member of components/FocusScreen/"
      contains: "Great session!"
      min_lines: 60
    - path: "mobile/src/__tests__/useFocusScreenState.test.js"
      provides: "Hook state-transition coverage for FOCUS-04"
      contains: "renderHook"
      min_lines: 90
    - path: "mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx"
      provides: "Render + interaction coverage for the done container per D-01"
      contains: "fireEvent"
      min_lines: 30
  key_links:
    - from: "mobile/src/screens/FocusScreen.jsx"
      to: "mobile/src/hooks/useFocusScreenState.js"
      via: "the screen's only hook call; everything it renders comes from this return value"
      pattern: "useFocusScreenState(navigation)"
    - from: "mobile/src/hooks/useFocusScreenState.js"
      to: "mobile/src/hooks/useFocusSession.js"
      via: "wrap-don't-absorb composition — the new hook calls the existing domain hook and spreads its return"
      pattern: "useFocusSession(uid, earnRP, handlePomodoroComplete)"
    - from: "mobile/src/__tests__/useFocusScreenState.test.js"
      to: "mobile/src/__tests__/rntlSmoke.test.js"
      via: "the awaited renderHook/act shape Phase 1 proved works under jest-expo 54 / React 19.1.0"
      pattern: "await renderHook("
---

<objective>
Stand up the extraction skeleton for FocusScreen end to end: write the pre-extraction behavior baseline, create the `useFocusScreenState` orchestration hook that wraps the existing `useFocusSession`, reduce `mobile/src/screens/FocusScreen.jsx` to a single hook call, and prove the whole path works by moving the smallest render branch (done phase) into the first real component under `mobile/src/components/FocusScreen/` — with hook state-transition tests and a component render/interaction test alongside it.

**As a** developer who has to safely decompose four 900-to-1700-line screens, **I want to** see one complete slice of FocusScreen run screen → orchestration hook → domain hook → named sub-component with tests attached, **so that** the remaining branches (and Phases 3-5) copy a shape that is already proven in this repo rather than one that is proposed on paper.

Purpose: FOCUS-02's "single hook" requirement and FOCUS-03's "named sub-components" requirement are architectural; if the hook contract is wrong, every later plan in this phase inherits the mistake. This plan fixes the contract first and validates it against real render output.

Output: `02-CHARACTERIZATION.md`, `useFocusScreenState.js`, a thinner `FocusScreen.jsx`, `FocusDoneScreen.jsx`, and two test files.

**This is the tracer slice.** It wires one path through every layer this phase touches — characterization doc → orchestration hook → the untouched domain hook → screen → a named component in the new directory → hook test → component test → green `npm run check`. It is production-quality, not a throwaway: nothing here is rewritten by a later plan, only expanded outward from.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md
@.planning/phases/02-focusscreen-decomposition/02-RESEARCH.md
@.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md
@.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md
</context>

<planning_notes>
**Capability checkpoints, run this planning session:**

- API-coverage detector returned `detected: false` over the phase scope — this phase touches no external API/SDK/service. Skipped, no `COVERAGE.md`.
- Assumption-delta detector returned `detected: false` — no singular→plural / required→optional / derived→chosen transition in scope. Skipped.
- Schema-push gate: no ORM/schema files (Payload, Prisma, Drizzle, Supabase, TypeORM) appear anywhere in this phase's file set. Skipped, no `[BLOCKING]` task.
- Package-legitimacy gate: no package-manager install task exists in this phase; `02-RESEARCH.md` records the audit as "not applicable — no new external packages".

**Deviations from `02-RESEARCH.md`, decided by the planner and deliberate:**

1. **Hook signature is `useFocusScreenState(navigation)`, not `(uid, navigation)`.** RESEARCH.md's own code sample calls `useAuthContext()` *inside* the hook while also taking `uid` as a parameter — the two are redundant. Taking only `navigation` is what lets the screen stop importing `AuthContext` entirely, which is the actual FOCUS-02 bar ("no business logic left inline").
2. **Containers receive no `navigation` prop.** The hook exposes `goBack()`, `openHistory()`, and `confirmStopAndGoBack()` so that every component under `components/FocusScreen/` is pure presentation driven by callback props. This is what makes D-01's interaction tests possible with plain `jest.fn()` props.
3. **`handleStopConfirm` is parameterized, not deduped into one callback.** See the `must_haves` truth on this — RESEARCH.md Open Question 2 asserts the two `Alert.alert` call sites are byte-identical; they are not (one dispatches the intercepted navigation action, the other goes back).
4. **`TaskList` (setup) and `ActiveTaskList` (active) are two components, not one with a `variant` prop** — RESEARCH.md Open Question 1's own recommendation. Extracted in plans 02-07 and 02-05 respectively.

**Edge-coverage disposition (spec-less fallback, 10 probe rows, zero silent drops).** The probe engine is shape-based and classified all 10 rows `unresolved`; each is dispositioned here and nothing is dropped:

| Req | Probe category | Disposition |
|-----|----------------|-------------|
| FOCUS-01 | unclassified | Lifted — the "does a filter/search/category exist at all" question becomes an explicit finding the characterization notes must record (truth #2 above, Task 1). |
| FOCUS-02 | unclassified | Lifted — "screen holds no logic" becomes the single-hook-call truth (this plan) and the line-count/construct gate in plan 02-04. |
| FOCUS-03 | unclassified | Lifted — "named sub-components exist under `components/FocusScreen/`" becomes the artifact list, tracked per plan. |
| FOCUS-04 | adjacency | Lifted — `pomodoroCount` vs `sessionGoal` equality boundary; zero side is an explicit truth, equality side is a `backstop` truth (Task 2). |
| FOCUS-04 | empty | Lifted — first-mount return shape of the hook (explicit truth, Task 2). |
| FOCUS-04 | ordering | Lifted — todos array order preserved across `toggleTodo` (explicit truth, Task 2). |
| FOCUS-05 | adjacency | Lifted in plan 02-06 — chip "active" state is decided by exact string equality (`subject === chip.emoji + ' ' + chip.label`), so equality is the boundary that matters for pickers. |
| FOCUS-05 | empty | Lifted in plans 02-05 and 02-07 — a task list with zero rows renders nothing and adds no placeholder copy (matches UI-SPEC "empty" row). |
| FOCUS-05 | ordering | Lifted in plans 02-05 and 02-07 — list components render rows in the order of the array they are given. |
| FOCUS-06 | unclassified | Lifted in plan 02-08 — the manual parity pass is scored against `02-CHARACTERIZATION.md` + `02-UI-SPEC.md` via a `<human-check>`. |

**UI-SPEC `## UI Considerations` lift (8 rows, zero silent drops):** `empty`/`zero-one-many` (tasks) → plan 02-07 truths; `zero-one-many` (history link) → plan 02-04 truth; `zero-one-many` (pomodoro count display) and `overflow` (160px capped active task list) → plan 02-05 truths; `long-text` (single-line `TextInput`) → plan 02-07 truth. The two `⚠ unresolved` rows (`loading`, `error`) are recorded here as explicit planner assumptions: **no loading state and no error state exist in FocusScreen today, and this phase adds neither** — they are surfaced so no executor reads their absence as an omission to fix. Both appear in every plan's prohibition set via the "no behavior change" item.

**Canon-referral breadcrumb (prohibition probe):** OWASP / injection / path-traversal / prototype-pollution items are canon — covered by `/gsd-secure-phase` and lint tooling, and inapplicable to a client-side structural refactor that adds no trust boundary. Not minted here.
</planning_notes>

<artifacts_this_phase_produces>
Symbols and files created by **Phase 2** (this plan's contributions marked ★):

| Symbol / file | Kind | Path | Plan |
|---|---|---|---|
| ★ `useFocusScreenState(navigation)` | named export, hook | `mobile/src/hooks/useFocusScreenState.js` | 02-01 |
| ★ `BACKGROUNDS` | named export, const array | `mobile/src/hooks/useFocusScreenState.js` | 02-01 |
| ★ `SESSION_GOAL_PET_MESSAGES` | module-local const | `mobile/src/hooks/useFocusScreenState.js` | 02-01 |
| ★ `FocusDoneScreen` | default export, component | `mobile/src/components/FocusScreen/FocusDoneScreen.jsx` | 02-01 |
| `FocusActiveScreen`, `BigPetDisplay` | default exports, components | `mobile/src/components/FocusScreen/` | 02-02 |
| `SessionSummaryStats`, `DoneActions`, `GoalCelebrationModal` | default exports, components | `mobile/src/components/FocusScreen/` | 02-03 |
| `FocusSetupScreen` | default export, component | `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` | 02-04 |
| `ActiveSessionHeader`, `TimerControls`, `ActiveTaskList`, `PomodoroCycleDots` | default exports, components | `mobile/src/components/FocusScreen/` | 02-05 |
| `SubjectPicker`, `DurationPicker`, `GoalPicker` | default exports, components | `mobile/src/components/FocusScreen/` | 02-06 |
| `TaskInput`, `TaskList`, `SoundPicker`, `BackgroundPicker` | default exports, components | `mobile/src/components/FocusScreen/` | 02-07 |
| ★ `mobile/src/__tests__/useFocusScreenState.test.js` | test file | — | 02-01 |
| ★ `mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx` | test file | — | 02-01 |
| 17 further `*.test.jsx` files | test files | `mobile/src/components/FocusScreen/__tests__/` | 02-02 … 02-07 |
| ★ `02-CHARACTERIZATION.md` | doc | `.planning/phases/02-focusscreen-decomposition/` | 02-01 |
| `02-EXTRACTION-TEMPLATE.md` | doc | `.planning/phases/02-focusscreen-decomposition/` | 02-08 |

Removed by this phase: the module-local `BigPet` function in `mobile/src/screens/FocusScreen.jsx` (relocated as `BigPetDisplay` in 02-02), and that file's `makeStyles` factory (sliced across the extracted components).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-extraction behavior baseline (FOCUS-01)</name>

  <precondition>The npm registry is reachable — `npm ping` exits 0. `mobile/node_modules` is gitignored and may be absent from a fresh worktree, so the suite cannot run until an install completes.</precondition>

  <files>.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md</files>

  <read_first>
    - `mobile/src/screens/FocusScreen.jsx` — read the whole file. You are documenting what it does today; every claim in the notes must come from this read, not from the requirement text.
    - `mobile/src/hooks/useFocusSession.js` — read the whole file. It owns the phase machine, the todos array, history persistence, and the RP math the screen displays.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` section "UI Considerations" and "Copywriting Contract" — the visual half of the parity baseline already exists there; do not duplicate it, reference it.
    - `.planning/phases/02-focusscreen-decomposition/02-RESEARCH.md` section "Pitfall 1" — explains why FOCUS-01's wording does not match the code.
  </read_first>

  <action>
    Create `.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md`. Depth target is
    "enough for a human to score FOCUS-06 parity against", not exhaustive prose — per 02-CONTEXT.md's
    Claude's-Discretion note on characterization format.

    Required sections, in this order:

    1. `## Requirement-Text Correction` — state plainly that FOCUS-01's words "filter, search, and
       empty-category behavior" describe features that do not exist in this screen. Record the
       evidence: a whole-file read plus `grep -niE "search|filter|category" mobile/src/screens/FocusScreen.jsx mobile/src/hooks/useFocusSession.js`,
       whose only hit is a derived count computation on the done screen. Name
       `.planning/codebase/CONCERNS.md` as the origin of the inherited wording. State that the notes
       below document the real analogs instead, and that no such feature is to be built.

    2. `## Phase Branches` — the three mutually exclusive render branches and the exact condition
       that selects each: done when phase is `done`; active when phase is `focus`, `break`, or
       `paused`; setup otherwise. Note which chrome each branch shows (close button, header,
       companion) and which it omits.

    3. `## Selection state machines` — the real "filter-shaped" behavior. One short subsection each
       for subject chips, duration presets, session-goal chips, sound chips, and background swatches.
       For each: the option source (`SUBJECT_CHIPS`, `FOCUS_PRESETS`, the literal 0-5 range,
       `SOUND_OPTIONS`, `BACKGROUNDS`), the default at first mount, how "active" is decided (note that
       subject compares an assembled `emoji + ' ' + label` string while the others compare an `id`),
       and that every one is single-select with no way to return to "nothing selected" except the
       goal chip labelled None. Record the custom-subject path separately: choosing Other clears the
       subject and opens a text field; the field commits on submit AND on blur; a whitespace-only
       value commits nothing.

    4. `## Tasks list` — adding requires non-blank text, trims it, appends to the end, and clears the
       field; the Add affordance only appears once the field has non-blank content. Both the setup
       rows and the active rows only ever toggle `done` — record explicitly that the ✕ on a setup row
       is a toggle and that no delete capability exists anywhere. Empty list renders zero rows and no
       placeholder copy. The active list is height-capped and scrolls.

    5. `## Session lifecycle and pet reactions` — start/pause/resume/skip/stop/reset and what each
       does to the visible phase; the four transition-triggered buddy messages with their exact
       strings and their exact clear delays; the goal-celebration trigger condition and its delay
       before the modal appears; and the fact that a goal of None never celebrates.

    6. `## Leaving an active session` — the two confirmation paths. Record that both show the same
       dialog title, message, and two buttons, and that they differ in what happens after stopping:
       the back-gesture path completes the intercepted navigation action, the header close button
       goes back. Record that the header's other stop affordance stops immediately with no dialog.

    7. `## Not present in this screen` — one line each for: no search, no filter control, no category
       concept, no loading or spinner state, no error or failure UI, no empty-state copy, no delete
       for tasks. Cross-reference the two `⚠ unresolved` rows in 02-UI-SPEC.md's UI Considerations
       table and state that this phase adds none of them.

    8. `## How to score FOCUS-06` — a short numbered checklist a human can walk in the running app,
       one line per section above.

    Commit this file on its own, before touching any source file. The whole point of a
    characterization baseline is that it was written without knowledge of the new implementation.
  </action>

  <verify>
    <automated>test -f .planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md && grep -c -e '## Requirement-Text Correction' -e '## Phase Branches' -e '## Selection state machines' -e '## Tasks list' -e '## Session lifecycle and pet reactions' -e '## Leaving an active session' -e '## Not present in this screen' -e '## How to score FOCUS-06' .planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md</automated>
    <fails_when>non-zero exit (file absent), or the count printed is less than 8 — one or more required sections is missing.</fails_when>
    <automated>S=$(git status --porcelain mobile/src) || exit 1; test -z "$S" && echo CLEAN || { echo "DIRTY:"; echo "$S"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `CLEAN` — source was touched before the baseline was recorded, which destroys the baseline's independence.</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` exists with all eight required `##` sections.
    - The notes name every one of the five single-select option sets and state the default for each.
    - The notes state that no search, filter, category, loading, error, empty-state copy, or task-delete capability exists in the screen today.
    - The notes record the two end-session paths as differing in their post-stop navigation.
    - `git log --oneline -1 -- .planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` returns a commit, and `git log --oneline -1 -- mobile/src/screens/FocusScreen.jsx` is older than it.
    - Nothing under `mobile/src` is modified by this task.
  </acceptance_criteria>

  <done>A committed, independent, pre-extraction description of FocusScreen's current behavior exists, and it corrects rather than inherits FOCUS-01's stale wording.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Create useFocusScreenState, reduce the screen to one hook call, and prove it with state-transition tests</name>

  <reversibility rating="costly">This hook's return shape is the contract every remaining plan in this phase — and, as the template-setter, Phases 3-5 — builds against. Changing it later means touching every extracted component and its test. Nothing is published or migrated, so it is costly rather than one-way.</reversibility>

  <files>mobile/src/hooks/useFocusScreenState.js, mobile/src/screens/FocusScreen.jsx, mobile/src/__tests__/useFocusScreenState.test.js</files>

  <read_first>
    - `mobile/src/screens/FocusScreen.jsx` lines 1-225 — the imports, the local state block, the three effects, and the three handler functions. This is the code you are relocating.
    - `mobile/src/hooks/useFocusSession.js` lines 62-110 and 250-388 — its parameter list, its control functions, and its exact return keys. You are spreading that return, so you must know it.
    - `mobile/src/hooks/useRP.js` lines 60-80 — confirms `useRP(uid)` and that `earnRP` comes off it.
    - `mobile/src/context/AuthContext.js` lines 40-51 and `mobile/src/context/PetContext.js` lines 20-30 — both throw when used outside their provider; that is why the test mocks them.
    - `mobile/src/__tests__/rntlSmoke.test.js` — the awaited `renderHook`/`act` shape Phase 1 proved under this exact jest-expo 54 / React 19.1.0 stack. Copy that shape.
    - `.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md` section "`mobile/src/hooks/useFocusScreenState.js` (hook, orchestration)" — import order, named-export convention, and the ref-mirror convention for values read inside timers.
  </read_first>

  <behavior>
    Assertions `mobile/src/__tests__/useFocusScreenState.test.js` must make. Write them first and
    watch them go red against the not-yet-existing module — that red is the evidence the file is
    exercising real code rather than passing vacuously.

    - Fresh mount returns `phase` `'idle'`, `isActive` false, `isDone` false, `todos` empty,
      `buddyMessage` null, `goalCelebModal` false, `showCustomInput` false, `todoInput` empty string,
      and `background` identical to the first entry of the exported `BACKGROUNDS` array.
    - Fresh mount exposes the option data the components need: `subjectChips`, `soundOptions`,
      `backgrounds`, and `FOCUS_PRESETS` are each non-empty arrays.
    - `start()` moves `phase` to `'focus'` and `isActive` to true.
    - `pause()` moves `phase` to `'paused'` and `isActive` stays true; `resume()` returns it to `'focus'`.
    - `stop()` moves `phase` to `'done'` and `isDone` to true; `reset()` returns `phase` to `'idle'`
      and empties `todos`.
    - The idle→focus transition sets `buddyMessage` to a non-null string containing the mocked pet's
      name, and advancing fake timers by 3000 ms clears it back to null.
    - `handleSubjectChip(chip)` sets `subject` to the chip's emoji, a single space, then its label,
      and leaves `showCustomInput` false with `customSubject` empty.
    - `showCustomSubjectInput()` sets `showCustomInput` true and empties `subject`.
    - `handleCustomSubject()` after `setCustomSubject('   ')` leaves `subject` unchanged; after
      `setCustomSubject('  Piano practice  ')` sets `subject` to the trimmed value.
    - `handleAddTodo()` with `todoInput` blank adds no row; with `'read ch 4'` appends one row whose
      `done` is false and clears `todoInput`.
    - After adding three rows and calling `toggleTodo` on the middle one, the array still reports the
      same three texts in the same order and only the middle row's `done` is true.
    - With `sessionGoal` set to 0, advancing fake timers well past a full focus interval leaves
      `goalCelebModal` false.
    - Driving one complete focus interval with `sessionGoal` set to 1 opens `goalCelebModal` after the
      celebration delay. Attempt this with fake timers; if the surrounding async settle (sound load,
      history write) cannot be made deterministic in this environment, do NOT weaken or delete the
      assertion — leave the test in place marked with the reason, record the flake in the plan
      SUMMARY, and let the `backstop` truth route it to the FOCUS-06 manual pass.
  </behavior>

  <action>
    Write the test file first, then the hook, then the screen edit.

    **`mobile/src/__tests__/useFocusScreenState.test.js`.** Flat directory placement alongside
    `useQuiz.test.js`, `camelCase.test.js` name, block comment header in this repo's house style
    naming what it guards and citing this plan. Mock four modules at file scope, with paths relative
    to `mobile/src/__tests__/`: `'../context/AuthContext'` exposing `useAuthContext` returning a user
    whose uid is `'test-uid'`; `'../context/PetContext'` exposing `usePetContext` returning
    `triggerReaction`, `studyBoost`, `say` as `jest.fn()`s plus a pet object with `petType` `'dog'`,
    a `name`, `chosen` true, and an empty accessories array; `'../hooks/useRP'` exposing `useRP`
    returning an `earnRP` `jest.fn()` — mocking this is what keeps Firestore and the RP singleton out
    of the test entirely; and `'@react-native-async-storage/async-storage'` resolved to that
    package's own shipped jest mock at `@react-native-async-storage/async-storage/jest/async-storage-mock`.
    Do NOT mock `useFocusSession` — the whole point is that real phase transitions run through it.
    Do NOT mock `ThemeContext` here; this hook never calls it.

    Build the navigation double as an object with `addListener` returning an unsubscribe `jest.fn()`,
    plus `dispatch`, `goBack`, and `navigate` as `jest.fn()`s — RESEARCH.md Pitfall 5 notes no such
    double exists anywhere in this repo yet, so this one is the precedent. Every `renderHook`,
    `render`, and `act` call is awaited (Phase 1's finding). Use `jest.useFakeTimers()` for the
    buddy-message clear and celebration assertions and restore real timers afterwards.

    **`mobile/src/hooks/useFocusScreenState.js`.** Named export, no default, matching
    `useFocusSession.js`. Move from `mobile/src/screens/FocusScreen.jsx` verbatim: the
    `SESSION_GOAL_PET_MESSAGES` table, the `BACKGROUNDS` array (export this one — components receive
    it as data, mirroring how `useFocusSession` already exports `SUBJECT_CHIPS` and `SOUND_OPTIONS`),
    the six local state declarations, the pomodoro-complete callback, the three effects, and the three
    handler functions. Preserve every delay value and every message string exactly as written; do not
    consolidate the six delayed-clear calls into a shared helper.

    Signature `useFocusScreenState(navigation)`. Inside, read the uid from the auth context, take
    `earnRP` from `useRP(uid)`, take `triggerReaction`, `studyBoost`, `say`, and `pet` from the pet
    context, then call `useFocusSession(uid, earnRP, handlePomodoroComplete)` and hold its return in a
    local. Spread that return into the object this hook returns — wrap it, do not copy its body in
    (02-RESEARCH.md Pattern 1; this hook-composing-a-hook call is the scoped, deliberate exception to
    the layer rule recorded in `.claude/CLAUDE.md`, and plan 02-08 writes it up as precedent).

    For the end-session dialog, define one module-or-hook-local helper that takes a single callback
    and shows the confirmation with the existing title, message, and two buttons — the second styled
    destructive — invoking the session stop and then that callback. Use it twice with different
    callbacks: the back-gesture effect passes one that dispatches the intercepted action, and the
    exposed `confirmStopAndGoBack` passes one that goes back. The dialog copy is shared; the two
    outcomes stay distinct.

    Return one flat object containing: everything spread from the session hook; the derived booleans
    `isActive` (phase is focus, break, or paused) and `isDone` (phase is done); the option data as
    `subjectChips`, `soundOptions`, and `backgrounds`; the screen-local values `buddyMessage`,
    `todoInput`, `setTodoInput`, `customSubject`, `setCustomSubject`, `showCustomInput`, `background`,
    `setBackground`, `goalCelebModal`, and `pet`; and the handlers `handleSubjectChip`,
    `showCustomSubjectInput`, `handleCustomSubject`, `handleAddTodo`, `clearBuddyMessage`,
    `dismissGoalCeleb`, `goBack`, `openHistory`, and `confirmStopAndGoBack`. `openHistory` navigates
    to `FocusHistory` passing the current history, exactly as the screen does today at three call
    sites. This return shape is the contract plans 02-02 through 02-07 consume — treat it as frozen.

    **`mobile/src/screens/FocusScreen.jsx`.** Delete the relocated state, effects, handlers, constants,
    and the now-unused context and RP imports. Call `useFocusScreenState(navigation)` once and read
    everything the remaining inline branches need off that single value. The active and setup branches
    keep their JSX inline for now — plans 02-02 and 02-04 move them. The window-dimensions read stays
    where the active branch uses it until 02-02 relocates it. The style factory and the local BigPet
    component stay until their consumers move. Do not otherwise reorder or reformat the JSX: a diff
    that only shows removals plus one hook call is what makes this reviewable.
  </action>

  <verify>
    <automated>npm --prefix mobile install && npm --prefix mobile test -- src/__tests__/useFocusScreenState.test.js 2>&1 | tail -40</automated>
    <fails_when>non-zero exit; or the output contains `Cannot find module`; or the summary line reports any failing test; or the output contains neither `Tests:` nor `Test Suites:` (jest never reached the file).</fails_when>
    <automated>grep -c 'useFocusSession(' mobile/src/hooks/useFocusScreenState.js</automated>
    <fails_when>the printed count is 0 — the orchestration hook is not wrapping the domain hook, meaning its body was absorbed instead.</fails_when>
    <automated>D=$(git diff --stat -- mobile/src/hooks/useFocusSession.js) || exit 1; test -z "$D" && echo UNCHANGED || { echo "MODIFIED:"; echo "$D"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `UNCHANGED` — the domain hook was modified, which this phase forbids.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/hooks/useFocusScreenState.js` exists, exports `useFocusScreenState` and `BACKGROUNDS` as named exports, and has no default export.
    - `grep -c 'export function useFocusScreenState' mobile/src/hooks/useFocusScreenState.js` prints 1.
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' mobile/src/screens/FocusScreen.jsx | grep -cE 'useAuthContext|usePetContext|useRP\(' ` prints 0 — the screen reads no context or RP state directly any more.
    - `grep -c 'useFocusScreenState(navigation)' mobile/src/screens/FocusScreen.jsx` prints 1 — exactly one orchestration hook call site.
    - `npm --prefix mobile test -- src/__tests__/useFocusScreenState.test.js` passes with at least 12 assertions covering the behaviors listed above.
    - `git diff -- mobile/src/hooks/useFocusSession.js` is empty.
    - The initial red run of the test file (before the hook existed) is recorded verbatim in the plan SUMMARY.
  </acceptance_criteria>

  <done>FocusScreen calls one hook; that hook owns the screen's former logic, wraps the untouched domain hook, and its state transitions are proven by an awaited RNTL test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract FocusDoneScreen as the first named sub-component, with a render + interaction test</name>

  <files>mobile/src/components/FocusScreen/FocusDoneScreen.jsx, mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx, mobile/src/screens/FocusScreen.jsx</files>

  <read_first>
    - `mobile/src/screens/FocusScreen.jsx` — the done branch (the block guarded by the done-phase check, roughly lines 227-335 before Task 2's edits) and the style factory block at the end of the file. This JSX is what moves.
    - `mobile/src/components/ActionChipRow.jsx` — 48 lines, the house shape for a component in this repo: theme read inside the component, local style factory, callback props.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` sections "Copywriting Contract", "Color", "Spacing Scale" — the parity lock. Off-grid values stay off-grid.
    - `mobile/src/__tests__/rntlSmoke.test.js` lines 45-71 — the awaited `render()`/`screen` shape.
  </read_first>

  <behavior>
    Assertions `mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx` must make.
    This component takes callback props, so per **D-01** it gets an interaction test, not render-only —
    and per **D-02** the bug class that matters here is a prop wired to the wrong handler, which only
    firing the callbacks can catch.

    - Renders the done heading copy and the computed minute total for a session with two completed
      pomodoros on the 25-minute preset plus three partial minutes.
    - Renders the earned-RP figure and the pomodoro multiplier row.
    - Renders the tasks-completed line when the session had tasks, and omits it entirely when the
      todos array is empty.
    - Pressing the primary action calls the reset callback exactly once and the history callback not
      at all; pressing the secondary action calls the history callback exactly once and reset not at all.
    - Pressing the close control calls the go-back callback exactly once.
    - Renders without the subject badge when subject is an empty string.
  </behavior>

  <action>
    Create `mobile/src/components/FocusScreen/` and its `__tests__/` subdirectory — both are new.

    **`FocusDoneScreen.jsx`.** Default-exported component, per the repo's component convention. Move
    the done branch's JSX across unchanged: safe-area wrapper, close control, scroll container,
    celebration emoji, heading, conditional subject badge, the summary stat row with all four of its
    conditional cards, the conditional tasks-completed line, the two action buttons, the conditional
    companion, and the celebration modal. Compute the displayed minute total and the done/total task
    counts inside this component from props, exactly as the screen computed them inline.

    Read the theme inside the component (`useTheme` from `../../context/ThemeContext` — note the
    two-level path from `components/FocusScreen/`), and give the file its own local style factory
    holding only the keys this branch uses: the safe wrapper, the close control and its text, the
    done scroll container, the done emoji, the subject badge and its text, the summary row, the stat
    card and its emoji/value/sub text, both action buttons and their texts, the modal backdrop, the
    modal card, and the celebration button. Copy each value byte-for-byte from the screen's style
    factory. Do not import the shared button, card, shadow, or pill factories from `../../styles/duo`
    — this screen has never used them and pulling them in would be a visual change (02-UI-SPEC.md
    Component Inventory). Do import the typography scale from `../../styles/duo`, because the done
    branch already uses it.

    Props, all supplied by the orchestration hook's return: `subject`, `pomodoroCount`, `preset`,
    `partialMinutes`, `sessionRP`, `todos`, `pet`, `buddyMessage`, `sessionGoal`, `goalCelebModal`,
    plus the callbacks `reset`, `openHistory`, `goBack`, and `dismissGoalCeleb`. Wire the celebration
    modal's dismiss and its request-close to `dismissGoalCeleb`; the primary button to `reset`; the
    secondary to `openHistory`; the close control to `goBack`. No `navigation` prop — the hook already
    owns navigation.

    **The screen.** Replace the done branch's inline JSX with a single render of this component,
    spreading the hook's return value into it. Remove from the screen every style key and import the
    done branch was the only consumer of; leave anything the active or setup branches still use.

    **The test.** Mock `'../../../context/ThemeContext'` (three levels up from
    `components/FocusScreen/__tests__/`) so the theme read returns a color-token object — the theme
    read returns null outside a provider and the component would crash destructuring it
    (02-RESEARCH.md Pitfall 3). Mock `'../../StudyBuddyCompanion'` to a trivial component so the test
    stays focused on this container. Build props inline per test; award callbacks are plain
    `jest.fn()`s. Await `render()`; query by the visible copy strings the UI-SPEC pins.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the output contains `No tests found` — the new `__tests__` directory was not discovered.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>grep -c -e 'duoBtn' -e 'cardShadow' -e 'elevatedCard' -e 'pillTab' mobile/src/components/FocusScreen/FocusDoneScreen.jsx</automated>
    <fails_when>the printed count is greater than 0 — a shared style factory this screen has never used was pulled in, which is a visual change.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FocusScreen/FocusDoneScreen.jsx` exists with a default export and its own local style factory.
    - The file contains the literal copy strings `Great session!`, `New Session`, and `View History`, matching 02-UI-SPEC.md's Copywriting Contract.
    - `grep -c 'navigation' mobile/src/components/FocusScreen/FocusDoneScreen.jsx` prints 0 — the container is pure presentation.
    - `mobile/src/components/FocusScreen/__tests__/FocusDoneScreen.test.jsx` fires both action buttons and the close control and asserts each callback was called exactly once, per D-01.
    - `npm --prefix mobile test -- src/components/FocusScreen` passes.
    - `npm --prefix mobile run check` exits 0.
    - The done branch no longer appears as inline JSX in `mobile/src/screens/FocusScreen.jsx`.
  </acceptance_criteria>

  <done>The done phase renders from a named component under `components/FocusScreen/`, driven entirely by the orchestration hook, and covered by an interaction test per D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly introduced | This plan relocates already-shipped client-side UI code between files in `mobile/src`. No network call, no persisted-data shape, no auth or session logic, and no new user-input path is added. `useFocusSession.js` — sole owner of this feature's AsyncStorage key `@focusHistory_v1_${uid}`, its notification scheduling, and its `logActivity` Firestore write — is not modified by this phase. Structure-only extraction, no behavior change, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `handleCustomSubject` / `handleAddTodo` relocation into `useFocusScreenState` | low | mitigate | ASVS V5: the existing trim-before-use must survive the move at exactly the sites it happens today. Enforced by the two blank-input assertions in Task 2's `<behavior>` block. |
| T-02-02 | Information disclosure | new test fixtures in `mobile/src/__tests__/useFocusScreenState.test.js` | low | mitigate | Fixtures use the literal `test-uid` and a stub pet; no real uid, token, or Firestore document is embedded, and `useRP` is mocked so no live credential path is reachable from the test. |
| T-02-SC | Tampering | package-manager installs | low | accept | No install task exists in this phase — `npm --prefix mobile install` only restores the already-locked tree. `02-RESEARCH.md` records the Package Legitimacy Audit as not applicable: zero new external packages. No `[ASSUMED]`/`[SUS]` entries exist, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0 (jest suite plus babel parse-check of changed files).
- `npm --prefix mobile test -- src/__tests__/useFocusScreenState.test.js` and `npm --prefix mobile test -- src/components/FocusScreen` both pass.
- `git diff -- mobile/src/hooks/useFocusSession.js` is empty.
- `02-CHARACTERIZATION.md` is committed strictly before the first source commit.
- `git status --porcelain chromebook src` is empty — mobile-only change set, per the repo's one-app-per-commit rule.
</verification>

<success_criteria>
- FOCUS-01 satisfied: an independent pre-extraction baseline exists and corrects the stale requirement wording rather than inventing code to match it.
- FOCUS-02 partially satisfied: the screen calls exactly one hook and reads no context directly; plan 02-04 completes the "no inline JSX" half.
- FOCUS-03 started: `components/FocusScreen/` exists with its first named container.
- FOCUS-04 satisfied for the orchestration hook: state transitions, the empty/first-mount edge, the todos-ordering edge, and the zero side of the goal adjacency boundary are covered by automated assertions; the equality side is carried as a `backstop` truth.
- FOCUS-05 satisfied for `FocusDoneScreen`: it has callback props, so it has an interaction test per D-01.
</success_criteria>

<output>
Create `.planning/phases/02-focusscreen-decomposition/02-01-SUMMARY.md` when done. Record: the verbatim initial red run of the hook test, the final `useFocusScreenState` return-key list (the contract plans 02-02 to 02-07 consume), whether the goal-celebration equality assertion could be driven deterministically, and any behavior discrepancy noticed during the move — as a finding, not a fix.
</output>
