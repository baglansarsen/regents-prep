---
phase: 02-focusscreen-decomposition
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - mobile/src/components/FocusScreen/FocusActiveScreen.jsx
  - mobile/src/components/FocusScreen/ActiveSessionHeader.jsx
  - mobile/src/components/FocusScreen/PomodoroCycleDots.jsx
  - mobile/src/components/FocusScreen/TimerControls.jsx
  - mobile/src/components/FocusScreen/ActiveTaskList.jsx
  - mobile/src/components/FocusScreen/__tests__/ActiveSessionHeader.test.jsx
  - mobile/src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx
  - mobile/src/components/FocusScreen/__tests__/TimerControls.test.jsx
  - mobile/src/components/FocusScreen/__tests__/ActiveTaskList.test.jsx
autonomous: true
requirements: [FOCUS-03, FOCUS-05]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`PomodoroCycleDots` has zero callback props, so per **D-01** it gets a render/snapshot test only — this is the exact pure-display example 02-RESEARCH.md names in its Code Examples section"
    - "`ActiveSessionHeader`, `TimerControls`, and `ActiveTaskList` each fire a callback prop, so per **D-01** each gets an interaction test; **D-02** is the reason — all three are thin prop-forwarding wrappers with no local state, the exact shape where a wrong-handler mis-wire is invisible to hook tests"
    - "`PomodoroCycleDots` renders exactly four dots in index order and reproduces the existing three-way fill rule: positions before the current one take the solid accent, the current position takes the accent at partial opacity, and positions after it take the neutral fallback — the boundary is at equality with the current position (FOCUS-05 adjacency and ordering probes)"
    - "`ActiveSessionHeader` reproduces all three count states: the count-over-goal figure when a goal is set, the repeated tomato glyphs capped at eight when no goal is set but the count is above zero, and nothing at all when no goal is set and the count is zero — 02-UI-SPEC.md UI Considerations, `zero-one-many` row"
    - "`ActiveSessionHeader` keeps the two stop affordances distinct: the left control asks for confirmation before leaving, the right control stops immediately with no dialog"
    - "`TimerControls` reproduces the paused/running swap — the resume control appears only in the paused phase and the pause control only otherwise — and the skip label differs between a focus phase and a break phase"
    - "`ActiveTaskList` renders nothing at all for an empty task list and adds no placeholder copy, renders rows in the order of the array it is given, and keeps the 160-point height cap and scroll behavior (02-UI-SPEC.md `empty` and `overflow` UI Considerations; FOCUS-05 empty and ordering probes)"
    - "`npm --prefix mobile run check` exits 0"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. A discrepancy found during the move is recorded in the SUMMARY as a finding and left as-is — PROJECT.md is explicit that a behavior change discovered as 'needed' is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or `.todo`-ing any existing test, and MUST NOT ship a vacuous new one. A snapshot committed without ever reading it is proof of nothing; read each generated snapshot before committing it."
      category: transparency
    - statement: "MUST NOT slip opportunistic work into this structural refactor — no new feature, no UX fix, no empty-state copy for the task list where none exists today, no dependency change, no edits outside `mobile/`."
      category: values
    - statement: "MUST NOT substitute the shared button, card, shadow, or pill style factories for this screen's own local flat styles, and MUST NOT round the screen's off-grid spacing values to a tidier scale. Both are visual changes disguised as cleanup (02-UI-SPEC.md Component Inventory and Spacing Scale)."
      category: values
  artifacts:
    - path: "mobile/src/components/FocusScreen/ActiveSessionHeader.jsx"
      provides: "Active-session top bar: leave control, subject line, pomodoro count display, immediate-stop control (FOCUS-03)"
      contains: "onRequestStop"
      min_lines: 40
    - path: "mobile/src/components/FocusScreen/PomodoroCycleDots.jsx"
      provides: "Four-dot cycle indicator, pure display (FOCUS-03); the D-01 render-only tier example named in RESEARCH"
      contains: "cyclePosition"
      min_lines: 20
    - path: "mobile/src/components/FocusScreen/TimerControls.jsx"
      provides: "Pause/resume and skip transport controls (FOCUS-03)"
      contains: "onSkip"
      min_lines: 30
    - path: "mobile/src/components/FocusScreen/ActiveTaskList.jsx"
      provides: "Height-capped scrolling task list for the active session (FOCUS-03)"
      contains: "maxHeight"
      min_lines: 30
    - path: "mobile/src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx"
      provides: "Render/snapshot coverage for the pure-display tier per D-01"
      contains: "toMatchSnapshot"
      min_lines: 20
  key_links:
    - from: "mobile/src/components/FocusScreen/FocusActiveScreen.jsx"
      to: "mobile/src/components/FocusScreen/ActiveSessionHeader.jsx"
      via: "the container forwards the hook's two distinct stop callbacks to the header's two distinct controls"
      pattern: "ActiveSessionHeader"
    - from: "mobile/src/components/FocusScreen/FocusActiveScreen.jsx"
      to: "mobile/src/components/FocusScreen/ActiveTaskList.jsx"
      via: "the container passes the todos array and the toggle callback; the empty-list guard lives inside the list component"
      pattern: "ActiveTaskList"
---

<objective>
Split the active-session container into its four widgets — the top bar, the cycle indicator, the transport controls, and the task list — each tested at the depth **D-01** assigns it.

**As a** student mid-session, **I want to** the header, dots, controls, and task list to keep behaving identically, **so that** a structural change never costs me a running timer.

Purpose: this is where the active branch stops being one 150-line block and becomes four independently testable units, and where three of the phase's edge-coverage rows (`empty`, `ordering`, `adjacency` on rendered lists) get automated coverage rather than manual inspection.

Output: four components under `components/FocusScreen/`, four test files, and a `FocusActiveScreen` that composes rather than inlines.

This expands out from plan 02-02's container; it does not touch `mobile/src/screens/FocusScreen.jsx`, which is why it runs in parallel with plan 02-04.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md
@.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md
@.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md
@.planning/phases/02-focusscreen-decomposition/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See plan 02-01's identically named section for the full Phase 2 list. This plan contributes:

| Symbol / file | Kind | Path |
|---|---|---|
| `ActiveSessionHeader` | default export, component | `mobile/src/components/FocusScreen/ActiveSessionHeader.jsx` |
| `PomodoroCycleDots` | default export, component | `mobile/src/components/FocusScreen/PomodoroCycleDots.jsx` |
| `TimerControls` | default export, component | `mobile/src/components/FocusScreen/TimerControls.jsx` |
| `ActiveTaskList` | default export, component | `mobile/src/components/FocusScreen/ActiveTaskList.jsx` |
| Four matching `*.test.jsx` files | test files | `mobile/src/components/FocusScreen/__tests__/` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract ActiveSessionHeader and PomodoroCycleDots — one per D-01 tier</name>

  <files>mobile/src/components/FocusScreen/ActiveSessionHeader.jsx, mobile/src/components/FocusScreen/PomodoroCycleDots.jsx, mobile/src/components/FocusScreen/__tests__/ActiveSessionHeader.test.jsx, mobile/src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusActiveScreen.jsx` as plan 02-02 wrote it — the header block and the four-dot row, plus the style keys they use.
    - `mobile/src/components/FocusTimerRing.jsx` — the house shape for a pure-display component: default export, no callback props, module-scope style block.
    - `mobile/src/components/ActionChipRow.jsx` — the house shape for an interactive component.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` section "UI Considerations", `zero-one-many` row on the pomodoro count display — the three-state rule the header must reproduce, including the eight-repeat cap.
    - `.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md` section "Sub-Component Test Depth (FOCUS-05)" — **D-01** splits these two into different test tiers despite both being stateless.
  </read_first>

  <behavior>
    `ActiveSessionHeader.test.jsx` (interaction tier — it has callback props):
    - The subject line renders when a subject is set and is absent when the subject is an empty string.
    - With a goal above zero, the count-over-goal figure renders.
    - With no goal and a count above zero, the repeated tomato glyphs render instead, and a count above
      eight still renders exactly eight glyphs — the cap.
    - With no goal and a zero count, neither figure renders.
    - Pressing the left control calls the confirm-and-leave callback exactly once and the immediate-stop
      callback not at all.
    - Pressing the right control calls the immediate-stop callback exactly once and the confirm callback
      not at all.

    `PomodoroCycleDots.test.jsx` (render/snapshot tier — **zero callback props**, no `fireEvent`):
    - Exactly four dots render for every cycle position.
    - At cycle position two, the tree matches a committed snapshot showing two solid dots, one partial,
      and one neutral.
    - At cycle position zero, no dot is solid and the first is the partial one.
    - At cycle position three, three dots are solid.
  </behavior>

  <action>
    Create both components as default exports under `mobile/src/components/FocusScreen/`.

    `ActiveSessionHeader.jsx`: move the header row across unchanged — the left control with its
    translucent background, hit slop, and glyph; the centered column holding the conditional subject
    line and the three-state count display; and the right control with the same translucent background
    and its stop label. Keep the count display's exact ternary chain and its eight-repeat cap; keep both
    hit-slop objects. Module-scope style block holding the header row, the subject line, and the control
    and control-text keys, copied byte-for-byte. No theme read — the active branch draws from the scene
    colors, which arrive as props.

    Props: the subject string, the pomodoro count, the session goal, the scene-derived text color, the
    scene-derived muted color, and two distinct callbacks — one for the left control that asks for
    confirmation before leaving and one for the right control that stops immediately. Give them
    different names so a mis-wire is visible at the call site; they are genuinely different behaviors
    and must not be collapsed into one prop.

    `PomodoroCycleDots.jsx`: move the four-dot row across unchanged — the row container with its gap and
    bottom margin, the four-index map, and the three-way fill rule where positions before the current
    one take the solid accent, the current position takes the accent with the partial-opacity suffix
    appended, and later positions take the neutral fallback. Keep the dot dimensions and radius exactly.
    Props: the cycle position and the accent color. No callbacks — that is what places this component in
    D-01's render-only tier, and it is the example 02-RESEARCH.md names for that tier.

    Write both test files. Neither needs a theme mock (neither component reads the theme). Await every
    `render()`. For the header, use `fireEvent.press` and assert with `toHaveBeenCalledTimes` on both
    the callback that should fire and the one that should not. For the dots, assert the rendered dot
    count explicitly before taking the snapshot, so a drifting snapshot still fails a real assertion;
    read the generated snapshot before committing it. Do not use `fireEvent` in the dots test at all.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen/__tests__/ActiveSessionHeader.test.jsx src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 2 test suites, meaning one of the two paths did not resolve.</fails_when>
    <automated>grep -c 'fireEvent' mobile/src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx</automated>
    <fails_when>the printed count is greater than 0 — an interaction test on a component with no callback props means either the component grew a callback it should not have, or the test tier was misapplied against D-01.</fails_when>
    <automated>grep -cE 'onPress|onChange|onSubmit' mobile/src/components/FocusScreen/PomodoroCycleDots.jsx</automated>
    <fails_when>the printed count is greater than 0 — the pure-display component acquired an interactive affordance it never had.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both components exist with default exports.
    - `ActiveSessionHeader.jsx` declares two differently named stop callbacks and wires them to two different controls.
    - `PomodoroCycleDots.jsx` renders a fixed four-position map and takes no callback props.
    - `ActiveSessionHeader.test.jsx` asserts all three count states including the eight-glyph cap.
    - `PomodoroCycleDots.test.jsx` asserts the dot count at three different cycle positions and has a committed snapshot.
    - `npm --prefix mobile test -- src/components/FocusScreen/__tests__/ActiveSessionHeader.test.jsx src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx` passes.
  </acceptance_criteria>

  <done>The active header and cycle indicator are named components, each covered at the tier D-01 assigns it, with the three header count states and the dot-fill boundary pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract TimerControls and ActiveTaskList</name>

  <files>mobile/src/components/FocusScreen/TimerControls.jsx, mobile/src/components/FocusScreen/ActiveTaskList.jsx, mobile/src/components/FocusScreen/__tests__/TimerControls.test.jsx, mobile/src/components/FocusScreen/__tests__/ActiveTaskList.test.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusActiveScreen.jsx` as plan 02-02 wrote it — the controls row and the capped task list, plus their style keys.
    - `mobile/src/components/ActionChipRow.jsx` — note its guard that returns nothing when there is nothing to show; the task list uses the same shape for its empty case.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` section "UI Considerations", the `empty` and `overflow` rows — zero tasks renders zero rows with no placeholder copy, and the height cap is exactly 160.
    - `.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md` section "Sub-Component Test Depth (FOCUS-05)" — both of these are interaction tier per **D-01**.
  </read_first>

  <behavior>
    `TimerControls.test.jsx`:
    - In a running phase the pause control renders and the resume control does not; pressing pause calls
      the pause callback exactly once and the resume callback not at all.
    - In the paused phase the resume control renders and the pause control does not; pressing resume
      calls the resume callback exactly once.
    - Pressing skip calls the skip callback exactly once in both phases.
    - The skip label reads one way during a focus phase and another during a break phase; both render.

    `ActiveTaskList.test.jsx`:
    - With three tasks, all three texts render in the order given, not sorted or reordered.
    - Pressing a row calls the toggle callback with that row's id — assert the argument, not just the
      call.
    - A task marked done renders with the struck-through treatment and the filled checkbox glyph; a task
      not done renders without them.
    - With an empty array, nothing renders — no row, no container, and no placeholder copy. Assert the
      absence of any text node, not merely the absence of a specific string.
  </behavior>

  <action>
    Create both components as default exports under `mobile/src/components/FocusScreen/`.

    `TimerControls.jsx`: move the controls row across unchanged — the row container with its gap and
    paddings, and the phase-conditional swap between a resume control on the scene accent and a pause
    control on the translucent overlay, followed by the skip control on its slightly lighter overlay
    with its phase-conditional label. Keep both overlay opacity values exactly as written; they are two
    different values and rounding them together would be a visual change. Module-scope style block with
    the row, button, and button-text keys copied byte-for-byte. No theme read.

    Props: the current phase, a break flag, the scene accent color, the scene-derived text color, the
    scene-derived muted color, and three callbacks for pause, resume, and skip.

    `ActiveTaskList.jsx`: move the capped list across unchanged — the scroll container with its 160-point
    height cap, horizontal padding, bottom margin, and hidden indicator, mapping each task to a pressable
    row with its checkbox and text. Keep the done-state treatment exactly: the checkbox border and fill
    switch to the scene accent and the check glyph appears; the text switches to the muted color and
    gains the struck-through decoration. Move the container's existing non-empty guard inside this
    component so it returns nothing for an empty array — that is render-equivalent to the guard's current
    position and matches the guard shape `ActionChipRow` already uses in this repo. Module-scope style
    block with the capped-list, row, checkbox, and text keys copied byte-for-byte. No theme read.

    Props: the task array, a toggle callback, the scene accent color, the scene-derived text color, and
    the scene-derived muted color.

    Write both test files. Neither needs a theme mock. Await every `render()`. Use `fireEvent.press`, and
    for the toggle assert the argument with `toHaveBeenCalledWith`. Flat `test()` calls with file-header
    block comments citing this plan.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen/__tests__/TimerControls.test.jsx src/components/FocusScreen/__tests__/ActiveTaskList.test.jsx 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 2 test suites.</fails_when>
    <automated>grep -c 'maxHeight: *160' mobile/src/components/FocusScreen/ActiveTaskList.jsx</automated>
    <fails_when>the printed count is 0 — the height cap was dropped or altered during the move, which is the `overflow` row of 02-UI-SPEC.md's UI Considerations.</fails_when>
    <automated>grep -c 'toHaveBeenCalledWith' mobile/src/components/FocusScreen/__tests__/ActiveTaskList.test.jsx</automated>
    <fails_when>the printed count is 0 — the toggle test counts calls without checking which task id was passed, which is the mis-wire D-02 is specifically about.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both components exist with default exports and no theme read.
    - `TimerControls.test.jsx` asserts the pause/resume swap in both directions and both skip labels.
    - `ActiveTaskList.test.jsx` asserts row order is preserved, asserts the toggle argument, and asserts that an empty array renders no text node at all.
    - `ActiveTaskList.jsx` keeps the 160-point cap and the two done-state treatments.
    - `npm --prefix mobile test -- src/components/FocusScreen/__tests__/TimerControls.test.jsx src/components/FocusScreen/__tests__/ActiveTaskList.test.jsx` passes.
  </acceptance_criteria>

  <done>The transport controls and active task list are named components with interaction tests that pin the empty, ordering, and done-state cases.</done>
</task>

<task type="auto">
  <name>Task 3: Recompose FocusActiveScreen from the four extracted widgets</name>

  <files>mobile/src/components/FocusScreen/FocusActiveScreen.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusActiveScreen.jsx` in its current state — you are removing four blocks and adding four renders.
    - `mobile/src/components/FocusScreen/__tests__/FocusActiveScreen.test.jsx` as plan 02-02 wrote it — it must keep passing unchanged; if it needs editing to stay green, the recomposition changed behavior.
    - The four components from Tasks 1 and 2 — their exact prop names.
  </read_first>

  <action>
    Replace the inline header, the inline four-dot row, the inline controls row, and the inline task list
    with single renders of the four components, passing the scene-derived colors the container already
    computes and forwarding the callbacks it already receives. Pass the container's two distinct stop
    callbacks to the header's two distinct control props — do not collapse them.

    Delete from this file every style key only the four moved blocks used: the header row, the subject
    line, the stop control and its text, the controls row, the control button and its text, the capped
    list, and the row, checkbox, and text keys. Keep the keys the container still renders itself: the pet
    area and the ring area.

    Do NOT edit `FocusActiveScreen.test.jsx`. It was written against rendered output, and rendered output
    is what must be unchanged. If it fails, the recomposition is wrong — fix the component, not the test.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 10 test suites, meaning a file in the directory was not discovered. Ten is the floor because plan 02-04 runs in the same wave and may or may not have landed its own suite yet.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>D=$(git diff --stat -- mobile/src/components/FocusScreen/__tests__/FocusActiveScreen.test.jsx) || exit 1; test -z "$D" && echo UNCHANGED || { echo "MODIFIED:"; echo "$D"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `UNCHANGED` — the container's existing test was edited, meaning rendered output changed and parity was not preserved.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FocusScreen/FocusActiveScreen.jsx` renders `ActiveSessionHeader`, `PomodoroCycleDots`, `TimerControls`, and `ActiveTaskList` and holds none of their JSX inline.
    - `grep -c -e 'activeHeader' -e 'controlBtn' -e 'activeTodos' -e 'todoCheck' mobile/src/components/FocusScreen/FocusActiveScreen.jsx` prints 0.
    - `mobile/src/components/FocusScreen/__tests__/FocusActiveScreen.test.jsx` is unmodified and still passes.
    - `npm --prefix mobile test -- src/components/FocusScreen` passes with all suites discovered.
    - `npm --prefix mobile run check` exits 0.
  </acceptance_criteria>

  <done>The active container composes four named widgets, its pre-existing test still passes untouched, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly introduced | Structure-only relocation of already-shipped client-side presentation. These four components render values supplied as props and forward presses to callbacks; none performs I/O, reads storage, or accepts user text input. `useFocusSession.js`, the only owner of persistence and notifications for this feature, is untouched by this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering | `ActiveSessionHeader` stop controls | low | mitigate | Collapsing the confirm-then-leave control and the immediate-stop control into one prop would let a session end without the confirmation the user expects. Mitigated by requiring two differently named callback props and by the mutually-exclusive call assertions in Task 1. |
| T-02-10 | Denial of service | `ActiveTaskList` height cap | low | mitigate | Losing the 160-point cap would let a long task list push the transport controls off screen mid-session, stranding the user with no way to pause or stop. Enforced by the cap grep in Task 2. |
| T-02-SC | Tampering | package-manager installs | low | accept | No install task exists in this plan. `02-RESEARCH.md` records the Package Legitimacy Audit as not applicable — zero new external packages, no `[ASSUMED]`/`[SUS]` entries, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0.
- `npm --prefix mobile test -- src/components/FocusScreen` passes with every suite in the directory discovered.
- `mobile/src/components/FocusScreen/__tests__/FocusActiveScreen.test.jsx` is byte-identical to the version plan 02-02 committed.
- `git status --porcelain chromebook src` is empty — mobile-only change set.
</verification>

<success_criteria>
- FOCUS-03 advanced: four more named sub-components under `components/FocusScreen/`, completing the active branch.
- FOCUS-05 advanced with both D-01 tiers applied correctly — one render/snapshot-only component and three interaction-tested ones.
- Three edge-coverage rows (FOCUS-05 `empty`, `ordering`, `adjacency`) and two UI-SPEC UI Considerations rows (`overflow`, `zero-one-many`) are pinned by automated assertions.
</success_criteria>

<output>
Create `.planning/phases/02-focusscreen-decomposition/02-05-SUMMARY.md` when done. Record: the final prop lists of the four components, confirmation that `FocusActiveScreen.test.jsx` passed unedited, and any behavior discrepancy noticed during the move — as a finding, not a fix.
</output>
