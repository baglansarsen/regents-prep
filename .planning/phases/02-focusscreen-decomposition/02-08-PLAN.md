---
phase: 02-focusscreen-decomposition
plan: 08
type: execute
wave: 6
depends_on: ["02-03", "02-05", "02-07"]
files_modified:
  - .planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md
autonomous: true
requirements: [FOCUS-01, FOCUS-06]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every `.jsx` component file under `mobile/src/components/FocusScreen/` has a matching `*.test.jsx` in that directory's `__tests__/` — this is FOCUS-05's real bar, checked mechanically rather than asserted"
    - "Every component that declares an interactive handler attribute has `fireEvent` in its test, and every component that declares none does not — **D-01**'s interactivity split, enforced as a gate rather than left as an authoring convention"
    - "`mobile/src/screens/FocusScreen.jsx` ends the phase at 35 lines or fewer, down from 908 at its start"
    - "`mobile/src/hooks/useFocusSession.js` is byte-identical to its state before Phase 2 began — the domain hook was wrapped, never edited"
    - "`npm --prefix mobile run check` exits 0 with the full suite green"
    - "A human has walked FocusScreen in the running app against `02-CHARACTERIZATION.md` and `02-UI-SPEC.md` and confirmed behavior and appearance match — FOCUS-06 is manual by design, because an automated snapshot only proves the new code matches itself, not that it matches the pre-extraction behavior"
    - "`02-EXTRACTION-TEMPLATE.md` records the reusable extraction recipe for Phases 3-5, including the hook-composition exception this phase establishes, so those phases apply it consistently instead of re-litigating it per screen"
    - "The requirement-text finding from FOCUS-01 (no filter, search, or category feature exists in this screen) is carried forward as an explicit note so the same stale wording in `.planning/codebase/CONCERNS.md` is not silently inherited by a later phase"
  prohibitions:
    - statement: "MUST NOT mark the manual parity pass complete without actually exercising the screen. A rubber-stamped FOCUS-06 is worse than an unfinished one: it converts an open question into a false record, and every later phase builds on that record."
      category: transparency
    - statement: "MUST NOT fix a behavior discrepancy found during the parity pass inside this phase. Record it as a finding with its reproduction steps and let the developer decide — PROJECT.md is explicit that a behavior change discovered as 'needed' is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT edit `.claude/CLAUDE.md`, `.planning/codebase/*.md`, or any other project instruction file to make the architecture rule match the new code. Recording the exception in the phase's own artifact is in scope; rewriting the project's standing instructions without the developer's decision is not."
      category: values
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or `.todo`-ing any test written during this phase."
      category: transparency
  artifacts:
    - path: ".planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md"
      provides: "The reusable screen-decomposition recipe Phases 3-5 copy, plus the precedents and findings this phase established"
      contains: "Hook composition"
      min_lines: 60
  key_links:
    - from: ".planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md"
      to: ".planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md"
      via: "the template's step one is 'write the characterization notes first'; this phase's own notes are the worked example"
      pattern: "02-CHARACTERIZATION.md"
---

<objective>
Close the phase: prove mechanically that every extracted component is tested at the depth **D-01** assigns it, confirm behavior and appearance parity by hand against the pre-extraction baseline, and write down the recipe so Phases 3-5 copy a proven procedure instead of re-deriving one.

**As a** developer about to repeat this on three more screens, **I want to** the FocusScreen extraction to end with a mechanically verified structure and a written recipe, **so that** FriendsScreen, QuizScreen, and HomeScreen cost a fraction of what this one did.

Purpose: FOCUS-06 is the only requirement in this phase that cannot be automated, and the extraction template is what turns one screen's work into four screens' worth of value — it is named directly in this phase's roadmap goal.

Output: a structural audit result, a completed manual parity pass, and `02-EXTRACTION-TEMPLATE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md
@.planning/phases/02-focusscreen-decomposition/02-RESEARCH.md
@.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md
@.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md
</context>

<artifacts_this_phase_produces>
See plan 02-01's identically named section for the full Phase 2 list. This plan contributes:

| Symbol / file | Kind | Path |
|---|---|---|
| `02-EXTRACTION-TEMPLATE.md` | doc | `.planning/phases/02-focusscreen-decomposition/` |

No source symbols are created or removed by this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical audit of the extracted structure and test tiers</name>

  <files>.planning/phases/02-focusscreen-decomposition/02-08-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/02-focusscreen-decomposition/02-01-SUMMARY.md` through `02-07-SUMMARY.md` — every finding recorded during the phase. You are collating them, and any that says "left as-is, flagged" must reappear in this plan's SUMMARY rather than disappearing.
    - `.planning/REQUIREMENTS.md` — the FOCUS-01 through FOCUS-06 text, to state which are closed by evidence and which by the manual pass.
  </read_first>

  <action>
    Run the four gates in the verify block below and record each command's verbatim output in this plan's
    SUMMARY — the numbers, not a paraphrase.

    Then collate findings. Read all seven prior plan SUMMARYs and build a single table in this plan's
    SUMMARY listing every discrepancy any executor recorded and left in place, with the file it was found
    in and a one-line description. If that table is empty, say so explicitly rather than omitting the
    section — "no discrepancies were found" and "nobody looked" are different claims and the record must
    distinguish them.

    Finally, state per requirement which evidence closes it: FOCUS-01 by the committed baseline document
    and its commit ordering; FOCUS-02 by the screen's line count and the comment-stripped construct gate;
    FOCUS-03 by the component-file count; FOCUS-04 by the hook test suite; FOCUS-05 by the pairing gate
    and the tier gate; FOCUS-06 by Task 2's manual pass.
  </action>

  <verify>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>for f in mobile/src/components/FocusScreen/*.jsx; do b=$(basename "$f" .jsx); test -f "mobile/src/components/FocusScreen/__tests__/$b.test.jsx" || echo "MISSING-TEST $b"; done</automated>
    <fails_when>any line starting with `MISSING-TEST` is printed — a component was extracted without a test, which is exactly the FOCUS-05 gap this phase exists to close.</fails_when>
    <automated>for f in mobile/src/components/FocusScreen/*.jsx; do b=$(basename "$f" .jsx); t="mobile/src/components/FocusScreen/__tests__/$b.test.jsx"; if grep -qE 'onPress=|onChangeText=|onSubmitEditing=|onRequestClose=' "$f"; then grep -q 'fireEvent' "$t" || echo "D01-GAP $b"; else grep -q 'fireEvent' "$t" && echo "D01-OVER $b"; fi; done</automated>
    <fails_when>any line starting with `D01-GAP` is printed — an interactive component has only a render test, violating D-01. Any line starting with `D01-OVER` is printed — a component with no interactive attribute has an interaction test, meaning either it grew an affordance it never had or the tier was misapplied.</fails_when>
    <automated>wc -l < mobile/src/screens/FocusScreen.jsx</automated>
    <fails_when>the printed number is greater than 35 — the screen is not thin.</fails_when>
    <automated>L=$(git log --format='%h %s' -1 -- mobile/src/hooks/useFocusSession.js) || exit 1; echo "$L"</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the printed commit subject carries this phase's `(02-` scope prefix — meaning the domain hook was edited during Phase 2, which this phase forbids. The commit-scope convention is the signal here; if the repo's scopes have drifted, confirm by hand against the phase's first commit instead of trusting this line.</fails_when>
  </verify>

  <acceptance_criteria>
    - `npm --prefix mobile run check` exits 0.
    - The component/test pairing loop prints no `MISSING-TEST` line, and 18 component files and 18 test files are present under `mobile/src/components/FocusScreen/`.
    - The tier loop prints neither a `D01-GAP` nor a `D01-OVER` line.
    - `wc -l < mobile/src/screens/FocusScreen.jsx` prints 35 or less.
    - `mobile/src/hooks/useFocusSession.js` shows a zero-line diff across the whole phase.
    - The SUMMARY carries the verbatim output of all four commands plus a findings table (or an explicit statement that no discrepancies were recorded).
  </acceptance_criteria>

  <done>The extracted structure and its test tiers are verified mechanically, and every finding recorded across the phase is collated in one place.</done>
</task>

<task type="auto">
  <name>Task 2: Manual behavior and appearance parity pass (FOCUS-06)</name>

  <files>.planning/phases/02-focusscreen-decomposition/02-08-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` — specifically its "How to score FOCUS-06" checklist, which plan 02-01 wrote for exactly this moment.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` sections "Copywriting Contract", "Color", "Typography", "Spacing Scale" — the visual half of the baseline.
    - `CLAUDE.md` (repo root), the Mobile commands section — how to start the app on a simulator.
  </read_first>

  <action>
    Start the app (`npm --prefix mobile run ios`, or the Expo dev server plus a simulator) and walk the
    checklist from the characterization notes. Automate nothing here: the entire point of a
    characterization-based refactor check is that a human compares the running app against a description
    written before the code moved. An automated snapshot would only prove the new code matches itself.

    Walk, at minimum: every one of the five option sections selecting and deselecting as described;
    the custom-subject path via both the submit and blur commits; adding a task, toggling it, and
    confirming the trailing control toggles rather than removes; starting a session and watching the
    transition message appear and clear; pausing, resuming, and skipping; both ways of leaving an active
    session and what each does afterwards; reaching a session goal and seeing the celebration; and the
    done screen's stat cards, action buttons, and history link.

    Record the result in this plan's SUMMARY as a line per checklist item with a pass or fail and, for any
    fail, the exact reproduction steps and the characterization-note line it contradicts. Do not fix a
    failure here — record it and surface it. A structural refactor that changed behavior is a finding the
    developer decides on, not a defect for the executor to patch inside the same phase.

    If the app cannot be started in this environment, say so plainly in the SUMMARY and leave FOCUS-06
    open. An unrun check recorded as unrun is a correct outcome; an unrun check recorded as passed is not.
  </action>

  <verify>
    <human-check>
      Open FocusScreen in the running app and walk the "How to score FOCUS-06" checklist in
      `.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` end to end.

      1. Setup screen: tap through subject chips, the other-subject path, the three session lengths, all
         six goal options, the sound chips, and the six scene swatches. Each selects, and selecting one
         deselects the previous. The custom-subject field commits both on keyboard submit and on tapping
         away.
      2. Tasks: the add affordance appears only once you type something; a task you add appears at the
         bottom of the list; the trailing control toggles it rather than deleting it; with no tasks the
         section shows the entry row and nothing beneath it.
      3. Start a session: the buddy message appears and clears on its own; the scene fills the screen;
         the dots, ring, controls, and task list all look as they did before.
      4. Pause, resume, skip. Then leave via the swipe-back gesture and confirm the dialog appears; then
         return and leave via the header close control and confirm the same dialog appears. Note that the
         second stop control on the right ends the session immediately with no dialog.
      5. Done screen: the stat cards, the tasks-completed line, both buttons, and the history link.
      6. Compare copy, colors, type sizes, and spacing against `02-UI-SPEC.md` — anything that reads or
         sits differently than before is a regression, not a judgment call.

      Expected outcome: every item matches the pre-extraction notes. Report any mismatch with
      reproduction steps; do not fix it in this phase.
    </human-check>
  </verify>

  <acceptance_criteria>
    - The SUMMARY contains one pass/fail line per checklist item from the characterization notes.
    - Any failure carries reproduction steps and names the characterization-note line it contradicts.
    - No source file under `mobile/src` is modified by this task — `git status --porcelain mobile/src` is empty.
    - If the app could not be started, the SUMMARY says so and FOCUS-06 is left open rather than marked passed.
  </acceptance_criteria>

  <done>A human has compared the running screen against the pre-extraction baseline and the result — pass, fail, or unrun — is recorded honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Write the extraction template for Phases 3-5</name>

  <files>.planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md</files>

  <read_first>
    - All seven prior plan SUMMARYs in this phase directory — the recipe is derived from what actually happened, not from what was planned.
    - `.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md` section "Claude's Discretion" — it names exactly which choices this phase was asked to make and record as precedent.
    - `.claude/CLAUDE.md` section "Layers" — the standing rule that hooks are used by screens exclusively, which this phase's hook composition is a scoped exception to.
    - `.planning/ROADMAP.md` phases 3, 4, and 5 — the three screens that will copy this recipe, including the naming-collision note already flagged for Phase 4.
  </read_first>

  <action>
    Create `.planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md` with these sections:

    1. `## The recipe` — the ordered procedure this phase actually followed: write the characterization
       notes and commit them before any code moves; create the orchestration hook and reduce the screen to
       a single hook call; move each render branch into a container; split each container into widgets;
       audit the pairing and tier gates; run the manual parity pass. Note which steps can run in parallel
       and which cannot, and why (shared files force sequence).

    2. `## Hook composition` — the precedent. State that an orchestration hook may call exactly one
       existing domain hook and spread its return value, that this is a deliberate, scoped exception to the
       standing layer rule in `.claude/CLAUDE.md`, and why wrapping beats absorbing (the domain hook's
       highest-risk logic stays untouched and outside the parity surface). State that Phases 3-5 should
       apply it rather than re-deciding it. Flag for the developer that the standing rule's wording in
       `.claude/CLAUDE.md` now under-describes the codebase, and recommend they decide whether to amend it
       — do not amend it yourself.

    3. `## Hook contract shape` — the return-object shape that worked: the wrapped hook's keys spread in,
       derived phase booleans, option lists passed through as data, screen-local state, and named handlers
       including navigation wrappers so no component ever touches navigation. Note that this is what makes
       component tests possible with plain callback doubles.

    4. `## Component boundaries` — the rule this phase used: one container per render branch, grounded in
       the screen's own section comments, then one widget per style-factory grouping. Record the two
       judgment calls and their reasons — the two task-list variants kept separate rather than unified
       behind a flag, and the two chip rows kept separate rather than generalized.

    5. `## Test tiers` — restate D-01 concretely with this phase's counts: how many components landed in
       each tier, and the two mechanical gates from Task 1 that enforce it. Include the three context
       modules that need mocking and which of them throws versus returns nothing outside its provider.

    6. `## Traps this phase hit` — the requirement-text mismatch (no filter, search, or category feature
       exists in FocusScreen; the wording came from a stale line in `.planning/codebase/CONCERNS.md` and
       the same line describes the other three screens, so Phases 3-5 should re-verify their own
       requirement wording against the actual files before planning); the two end-session paths that look
       identical but differ in what they do after stopping; the similarly named section-label style factory
       and local style key; and any trap recorded in a prior SUMMARY.

    7. `## Cost` — the plan count, wave count, and component and test counts this screen required, as a
       calibration input for sizing Phases 3-5.

    Then add a `## Carry-forward` section listing every open item: findings left unfixed from Task 1's
    table, the FOCUS-06 result from Task 2, and the Phase 4 naming-collision note already in STATE.md.
  </action>

  <verify>
    <automated>test -f .planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md && grep -c -e '## The recipe' -e '## Hook composition' -e '## Hook contract shape' -e '## Component boundaries' -e '## Test tiers' -e '## Traps this phase hit' -e '## Cost' -e '## Carry-forward' .planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md</automated>
    <fails_when>non-zero exit (file absent), or the printed count is less than 8 — one or more required sections is missing.</fails_when>
    <automated>S=$(git status --porcelain .claude/CLAUDE.md .planning/codebase) || exit 1; test -z "$S" && echo CLEAN || { echo "DIRTY:"; echo "$S"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `CLEAN` — a project instruction or codebase-map file was edited, which this plan forbids; the exception is recorded in the phase artifact and left for the developer to decide on.</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md` exists with all eight required `##` sections.
    - The hook-composition section states the exception explicitly and recommends — without performing — an amendment to the standing layer rule.
    - The traps section names the requirement-text mismatch and tells Phases 3-5 to re-verify their own requirement wording against their own files.
    - The carry-forward section lists the FOCUS-06 result and every unfixed finding from Task 1's table.
    - `git status --porcelain .claude/CLAUDE.md .planning/codebase` is empty.
  </acceptance_criteria>

  <done>Phases 3-5 have a written, evidence-derived recipe plus the precedents and traps this phase paid for.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly introduced | This plan writes one planning document and runs read-only audits over source. It modifies no application code, no configuration, and no dependency. Structure-only phase close, no behavior change, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Repudiation | the FOCUS-06 manual pass record | low | mitigate | A parity pass recorded as passed but never actually run is a false record that every later phase inherits. Mitigated by requiring a pass/fail line per checklist item with reproduction steps for failures, and by explicitly permitting an "unrun" outcome so there is no incentive to fake a pass. |
| T-02-16 | Tampering | project instruction files | low | mitigate | Editing `.claude/CLAUDE.md` or the codebase maps to make the standing architecture rule match the new code would silently rewrite the project's guidance without the developer deciding. Enforced by the prohibition above and the clean-status gate in Task 3. |
| T-02-SC | Tampering | package-manager installs | low | accept | No install task exists in this plan. `02-RESEARCH.md` records the Package Legitimacy Audit as not applicable — zero new external packages across the whole phase, no `[ASSUMED]`/`[SUS]` entries, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0 with the full suite green.
- The component/test pairing loop and the D-01 tier loop both print nothing.
- `wc -l < mobile/src/screens/FocusScreen.jsx` prints 35 or less.
- `mobile/src/hooks/useFocusSession.js` shows a zero-line diff across the whole phase.
- The FOCUS-06 manual pass is recorded with a per-item result, including an honest "unrun" if the app could not be started.
- `git status --porcelain chromebook src .claude/CLAUDE.md .planning/codebase` is empty.
</verification>

<success_criteria>
- FOCUS-05 closed by mechanical evidence: every extracted component has a test, at the tier D-01 assigns it.
- FOCUS-06 closed by a recorded manual parity pass against `02-CHARACTERIZATION.md` and `02-UI-SPEC.md` — or honestly left open if the app could not be exercised.
- FOCUS-01's finding carried forward so Phases 3-5 re-verify their own requirement wording rather than inheriting the same stale description.
- The roadmap goal's second clause — "establishing the extraction template for the remaining three screens" — has a concrete artifact behind it.
</success_criteria>

<output>
Create `.planning/phases/02-focusscreen-decomposition/02-08-SUMMARY.md` when done. Record: the verbatim output of all four audit commands, the collated findings table across all seven prior plans (or an explicit statement that none were recorded), the per-item FOCUS-06 result, and the final component/test counts.
</output>
