---
phase: 02-focusscreen-decomposition
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - mobile/src/screens/FocusScreen.jsx
  - mobile/src/components/FocusScreen/FocusSetupScreen.jsx
  - mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx
autonomous: true
requirements: [FOCUS-02, FOCUS-03, FOCUS-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`mobile/src/screens/FocusScreen.jsx` is reduced to its final form: one call to `useFocusScreenState(navigation)` and a three-way branch that renders `FocusDoneScreen`, `FocusActiveScreen`, or `FocusSetupScreen`. No React state declaration, no effect, no style block, no native dialog, and no inline UI primitive remains in it — this is the moment FOCUS-02 is fully satisfied"
    - "`mobile/src/screens/FocusScreen.jsx` is at most 35 lines — the screen that was 908 lines at the start of this phase"
    - "The setup phase renders from `mobile/src/components/FocusScreen/FocusSetupScreen.jsx`, driven entirely by values and callbacks from `useFocusScreenState`, with no `navigation` prop reaching it"
    - "The past-sessions link renders only when at least one past session exists, and its label carries the plural suffix only above one — 02-UI-SPEC.md UI Considerations, `zero-one-many` row on session history"
    - "The keyboard-avoiding wrapper, its platform-conditional behavior, and the scroll container's tap-persistence setting survive the move unchanged — dropping any of them would break typing into the custom-subject or task fields on iOS"
    - "`FocusSetupScreen` takes callback props, so per **D-01** it gets an interaction test, and per **D-02** the failure that catches is a control wired to the wrong handler after a 200-line block was relocated"
    - "`npm --prefix mobile run check` exits 0"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. A discrepancy found during the move is recorded in the SUMMARY as a finding and left as-is — PROJECT.md is explicit that a behavior change discovered as 'needed' is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or `.todo`-ing any existing test, and MUST NOT ship a vacuous new one. A render test that only proves no exception was thrown certifies nothing while looking like proof."
      category: transparency
    - statement: "MUST NOT slip opportunistic work into this structural refactor — no new feature, no UX fix, no loading or error state where none exists today, no dependency change, no edits outside `mobile/`."
      category: values
    - statement: "MUST NOT perform this relocation with a repository-wide search-and-replace. CLAUDE.md records that a bulk rename across `mobile/src` once shipped a crashing TestFlight build; per-file edits only, with the parse-check run before committing."
      category: safety
  artifacts:
    - path: "mobile/src/components/FocusScreen/FocusSetupScreen.jsx"
      provides: "Setup-phase container component (FOCUS-03) — header, all five option sections, task entry, start action, history link"
      contains: "Start Focusing"
      min_lines: 120
    - path: "mobile/src/screens/FocusScreen.jsx"
      provides: "Final thin screen: one hook call, three-way branch, nothing else (FOCUS-02)"
      contains: "useFocusScreenState"
    - path: "mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx"
      provides: "Render + interaction coverage for the setup container per D-01"
      contains: "fireEvent"
      min_lines: 45
  key_links:
    - from: "mobile/src/screens/FocusScreen.jsx"
      to: "mobile/src/components/FocusScreen/FocusSetupScreen.jsx"
      via: "the default branch of the screen's three-way render, spread with the orchestration hook's return value"
      pattern: "FocusSetupScreen"
    - from: "mobile/src/components/FocusScreen/FocusSetupScreen.jsx"
      to: "mobile/src/hooks/useFocusScreenState.js"
      via: "every option list, selection value, and handler arrives as a prop from the hook's return"
      pattern: "onSelect"
---

<objective>
Move the setup branch out of the screen and finish the job: after this plan `mobile/src/screens/FocusScreen.jsx` is a three-line branch over a single hook call, and the 908-line screen this phase started with exists only as a hook plus named components.

**As a** student setting up a focus session, **I want to** pick a subject, length, goal, tasks, sound, and scene exactly as before, **so that** nothing about starting a session changed except where the code lives.

Purpose: FOCUS-02's second half — "the screen is reduced to calling the hook and rendering" — is only provable once the last inline branch is gone. This plan is where that becomes measurable rather than aspirational.

Output: `FocusSetupScreen.jsx`, its interaction test, and the final `FocusScreen.jsx`.

This expands out from plan 02-01's proven slice, applied to the third and largest branch. Plans 02-06 and 02-07 then split this container's inner widgets; this plan does the branch-level move only.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-focusscreen-decomposition/02-CONTEXT.md
@.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md
@.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md
@.planning/phases/02-focusscreen-decomposition/02-01-SUMMARY.md
@.planning/phases/02-focusscreen-decomposition/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See plan 02-01's identically named section for the full Phase 2 list. This plan contributes:

| Symbol / file | Kind | Path |
|---|---|---|
| `FocusSetupScreen` | default export, component | `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` |
| `FocusSetupScreen.test.jsx` | test file | `mobile/src/components/FocusScreen/__tests__/` |

Removed by this plan: the setup-branch JSX, the style factory, and every remaining UI import in `mobile/src/screens/FocusScreen.jsx`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract FocusSetupScreen and reduce the screen to its final form</name>

  <files>mobile/src/components/FocusScreen/FocusSetupScreen.jsx, mobile/src/screens/FocusScreen.jsx</files>

  <read_first>
    - `mobile/src/screens/FocusScreen.jsx` in its current state — the whole file. After plans 02-01 and 02-02 the only branch left inline is the setup one, plus the style factory. Read both fully before moving anything.
    - `.planning/phases/02-focusscreen-decomposition/02-01-SUMMARY.md` — the frozen `useFocusScreenState` return-key list. Every prop this container takes comes from there.
    - `mobile/src/components/FocusScreen/FocusActiveScreen.jsx` as plan 02-02 wrote it — the container shape to mirror (default export, props-only, no navigation access).
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` sections "Spacing Scale", "Typography", "Copywriting Contract" — the off-grid values that must not be rounded and the pinned copy.
    - `.planning/phases/02-focusscreen-decomposition/02-PATTERNS.md` section on the container components — the per-container style-slice rule.
  </read_first>

  <action>
    Create `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` as a default-exported component and
    move the setup branch across unchanged. The whole tree goes: the bottom-edge safe-area wrapper, the
    keyboard-avoiding wrapper with its platform-conditional behavior value, the scroll container with
    its hidden indicator and its handled tap-persistence setting, the header row with its title, the
    past-sessions text link and the close control, the five labelled option sections (subject with its
    other-option and conditional custom field, session length, session goal, tasks with its entry row
    and rows list, background sound, and scene), the start action, the conditional past-sessions
    summary link, and the fixed-height spacer at the bottom.

    Read the theme inside the container and keep a local theme-parameterized style factory holding only
    the keys this branch uses. Copy every value byte-for-byte out of the screen's factory, including the
    off-grid ones 02-UI-SPEC.md lists — the section-label top margin, the preset button vertical
    padding, the two input-row vertical paddings, and the swatch dimensions. Import the typography scale
    from `../../styles/duo` because the header and the link already use it; import nothing else from
    there, and in particular do not reach for the shared button, card, shadow, pill, or section-label
    factories. Note that the section-label factory in that module and this screen's own section-label
    style key are two different, similarly named things — keep the local key as-is (02-UI-SPEC.md
    Component Inventory flags exactly this trap).

    Props, all from the orchestration hook's return: `subjectChips`, `subject`, `showCustomInput`,
    `customSubject`, `presets` (the preset list off the hook), `preset`, `sessionGoal`, `todos`,
    `todoInput`, `soundOptions`, `sound`, `backgrounds`, `background`, and `history`; plus the
    callbacks `handleSubjectChip`, `showCustomSubjectInput`, `setCustomSubject`, `handleCustomSubject`,
    `setPreset`, `setSessionGoal`, `setTodoInput`, `handleAddTodo`, `toggleTodo`, `setSound`,
    `setBackground`, `start`, `openHistory`, and `goBack`. Wire the header link and the bottom summary
    link both to `openHistory`, and the header close control to `goBack` — no navigation access in this
    file.

    Then reduce `mobile/src/screens/FocusScreen.jsx` to its final form. What remains: the React import,
    the hook import, the three container imports, and a default-exported function that takes the
    navigation prop, calls the hook once, and returns the done container when the done flag is set, the
    active container when the active flag is set, and the setup container otherwise — each spread with
    the hook's return value. Delete everything else from the file: the remaining local declarations, the
    style factory, every UI-primitive and platform import, the constants, and any import no longer
    referenced. Target at most 35 lines.

    Per CLAUDE.md, make these edits file by file — no repository-wide substitution — and run the
    parse-check before committing.
  </action>

  <verify>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>grep -vE '^[[:space:]]*(//|\*|/\*)' mobile/src/screens/FocusScreen.jsx | grep -cE 'useState|useEffect|useCallback|StyleSheet|Alert\.alert|makeStyles|TouchableOpacity|TextInput|ScrollView|SafeAreaView'</automated>
    <fails_when>the printed count is greater than 0 — logic or inline UI is still living in the screen, so FOCUS-02 is not met. Comment lines are stripped first so a header comment describing the rule cannot invalidate its own gate.</fails_when>
    <automated>wc -l < mobile/src/screens/FocusScreen.jsx</automated>
    <fails_when>the printed number is greater than 35 — the screen is not thin.</fails_when>
    <automated>grep -c 'navigation' mobile/src/components/FocusScreen/FocusSetupScreen.jsx</automated>
    <fails_when>the printed count is greater than 0 — the container reaches for navigation instead of taking a callback.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` exists with a default export and its own theme-parameterized style factory.
    - It contains the pinned copy `▶  Start Focusing` and the five section labels `What are you studying?`, `Session length`, `Session goal`, `Tasks`, `Background sound`, and `Background`.
    - `grep -c -e 'duoBtn' -e 'cardShadow' -e 'elevatedCard' -e 'pillTab' -e 'sectionLabel(' mobile/src/components/FocusScreen/FocusSetupScreen.jsx` prints 0.
    - `mobile/src/screens/FocusScreen.jsx` contains exactly one `useFocusScreenState(navigation)` call and renders all three containers.
    - `wc -l < mobile/src/screens/FocusScreen.jsx` prints 35 or less.
    - The comment-stripped construct grep over `mobile/src/screens/FocusScreen.jsx` prints 0.
    - `npm --prefix mobile run check` exits 0.
  </acceptance_criteria>

  <done>FocusScreen.jsx is a hook call and a three-way branch; the setup branch lives in a named container with no navigation access.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interaction test for FocusSetupScreen</name>

  <files>mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx</files>

  <read_first>
    - `mobile/src/components/FocusScreen/FocusSetupScreen.jsx` as Task 1 wrote it — its prop list and the copy strings you will query by.
    - `mobile/src/hooks/useFocusSession.js` lines 12-34 — the real subject, preset, and sound option arrays, so the test fixtures use real records rather than invented shapes.
    - `mobile/src/components/FocusScreen/__tests__/FocusActiveScreen.test.jsx` as plan 02-02 wrote it — the props-factory and mocking shape to mirror.
    - `.planning/phases/02-focusscreen-decomposition/02-UI-SPEC.md` section "UI Considerations" — the history `zero-one-many` and the task `empty` rows this test pins.
  </read_first>

  <behavior>
    Assertions this file must make. This container takes callback props, so **D-01** puts it in the
    interaction tier; **D-02**'s rationale applies directly — a 200-line relocation is exactly where a
    control silently ends up on the wrong handler.

    - All five section labels and the start action label render.
    - Pressing a subject chip calls the subject handler with that chip record.
    - Pressing the other-subject chip calls the show-custom-input handler, and with the custom-input
      flag on, the custom text field renders with its placeholder.
    - Pressing a length option calls the preset setter with that preset record.
    - Pressing a goal option calls the goal setter with that number; the zero option renders as the
      none label rather than a numeric one.
    - Typing into the task field calls the task-input setter with the typed text; submitting it calls
      the add handler.
    - The add affordance is absent when the task field is blank and present when it holds non-blank text.
    - With three tasks, all three texts render; pressing a task's toggle control calls the toggle
      handler with that task's id.
    - With zero tasks, no task row renders and no placeholder copy appears in its place.
    - Pressing a sound option calls the sound setter with that option record; pressing a scene swatch
      calls the scene setter with that scene record.
    - Pressing the start action calls the start callback exactly once.
    - With an empty history, the bottom summary link does not render; with one past session the label
      has no plural suffix; with two it does.
    - Pressing the header link and the bottom summary link both call the open-history callback.
  </behavior>

  <action>
    Write `mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx`.

    Mock `'../../../context/ThemeContext'` so the theme read returns a fixed color-token object — the
    read returns null outside a provider and the container would crash destructuring it
    (02-RESEARCH.md Pitfall 3). No other mock is needed: this container renders no native-module-backed
    child.

    Build a props factory local to the file returning a complete, valid prop set with `jest.fn()`
    callbacks, then override per test. Source the option arrays from the real exports in
    `mobile/src/hooks/useFocusSession.js` and the scene array from the export in
    `mobile/src/hooks/useFocusScreenState.js` rather than inventing fixtures — that way a future change
    to an option record breaks this test loudly instead of silently diverging.

    Await every `render()`. Use `fireEvent.press` for taps, `fireEvent.changeText` for the text field,
    and `fireEvent(node, 'submitEditing')` for the submit path. Assert callback arguments, not just
    call counts, wherever the handler takes one. Flat `test()` calls with a file-header block comment
    naming what this guards and citing this plan.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FocusScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the summary line reports fewer than 7 test suites, meaning a file in the directory was not discovered.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>grep -c 'toHaveBeenCalledWith' mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx</automated>
    <fails_when>the printed count is less than 5 — the test only counts calls instead of checking the argument each handler receives, which is the half of D-02's mis-wire risk that call counts cannot see.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FocusScreen/__tests__/FocusSetupScreen.test.jsx` exists and contains `fireEvent`.
    - It asserts the argument passed to at least the subject, preset, goal, sound, and scene handlers.
    - It asserts the add affordance's presence and absence across blank and non-blank field states.
    - It asserts the empty-task case renders no row and no substitute copy.
    - It asserts all three history states — absent at zero, singular at one, plural at two.
    - It sources option fixtures from the real exported arrays, not from literals invented in the test.
    - `npm --prefix mobile test -- src/components/FocusScreen` passes with all seven suites discovered.
    - `npm --prefix mobile run check` exits 0.
  </acceptance_criteria>

  <done>The setup container's prop wiring is proven by an interaction test that checks handler arguments, and two more UI-SPEC considerations are pinned automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client text entry → session state | The custom-subject field and the task field are the screen's only user-input surface. They existed before this phase and are unchanged by it; this plan relocates the fields without altering how their values are read, trimmed, or stored. No new boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Tampering | custom-subject and task text fields in `FocusSetupScreen` | low | mitigate | ASVS V5: the existing trim-before-use must stay where it happens today — inside the hook's handlers and the domain hook's add function, not moved into this component. `FocusSetupScreen` forwards raw text and nothing else. Enforced by the blank-field assertions in this plan's Task 2 and the trim assertions already in the hook test from plan 02-01. |
| T-02-08 | Tampering | whole-file relocation of a 200-line branch | low | mitigate | A bulk substitution across `mobile/src` once shipped a crashing build (CLAUDE.md). Mitigated by the per-file-edit prohibition above and by `npm --prefix mobile run check`, which parse-checks every changed file before commit. |
| T-02-SC | Tampering | package-manager installs | low | accept | No install task exists in this plan. `02-RESEARCH.md` records the Package Legitimacy Audit as not applicable — zero new external packages, no `[ASSUMED]`/`[SUS]` entries, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0.
- `npm --prefix mobile test -- src/components/FocusScreen` passes with all seven suites discovered.
- `wc -l < mobile/src/screens/FocusScreen.jsx` prints 35 or less, and the comment-stripped construct grep over it prints 0.
- `git diff -- mobile/src/hooks/useFocusSession.js` is empty.
- `git status --porcelain chromebook src` is empty — mobile-only change set.
</verification>

<success_criteria>
- FOCUS-02 fully satisfied: the screen calls one hook, renders from its return value, and holds no logic, styles, or inline UI.
- FOCUS-03 advanced: all three phase branches are now named containers under `components/FocusScreen/`.
- FOCUS-05 advanced: the setup container has an interaction test that checks handler arguments, per D-01.
- Two further UI-SPEC UI Considerations rows (history `zero-one-many`, task-list `empty`) are pinned by automated assertions.
</success_criteria>

<output>
Create `.planning/phases/02-focusscreen-decomposition/02-04-SUMMARY.md` when done. Record: the final line count of `mobile/src/screens/FocusScreen.jsx`, the final prop list of `FocusSetupScreen` (plans 02-06 and 02-07 split its widgets and need it), and any behavior discrepancy noticed during the move — as a finding, not a fix.
</output>
