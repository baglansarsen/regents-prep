---
phase: 03-friendsscreen-decomposition
plan: 06
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - mobile/src/components/FriendsScreen/ActivityRow.jsx
  - mobile/src/components/FriendsScreen/__tests__/ActivityRow.test.jsx
  - mobile/src/components/FriendsScreen/ActivityTab.jsx
  - mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx
autonomous: true
requirements: [FRIENDS-03, FRIENDS-05]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`mobile/src/components/FriendsScreen/ActivityRow.jsx` renders one feed row exactly as `ActivityTab` rendered it inline: the same tinted icon circle, the same actor line, the same relative-time text and the same body text, with every style value carried over unchanged (03-UI-SPEC.md parity lock)"
    - "The per-item derivation — the type-to-icon-and-colour lookup, the actor attribution chain, and the timestamp computation — stays in `ActivityTab` where it lives today, and is not moved into the row component"
    - "The type-to-icon-and-colour table stays defined exactly where it is today, inside the per-item mapping callback, rather than being hoisted to module scope — hoisting is a tidy-up this parity lock does not authorise"
    - "`ActivityRow` declares no callback props and therefore carries a render-tier test with no simulated interaction (D-01)"
    - "`ActivityTab` still renders rows in the order of the feed array it is given, adding no sort of its own (edge probe: FRIENDS-05 ordering)"
    - "`ActivityTab` still renders the `No Activity Yet` empty-state block and zero rows for an empty feed (UI-SPEC UI Considerations, `empty` / Activity tab)"
    - "The actor line still colours the current user's name with the brand colour and everyone else's with the default text colour"
    - "`npm --prefix mobile run check` exits 0"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. A discrepancy found during the split is recorded in the plan SUMMARY as a finding and left exactly as it is."
      category: values
    - statement: "MUST NOT add a press affordance, a navigation target, or any other interactivity to a feed row. The shipped row is a static view, and making it tappable would be a feature, not a refactor."
      category: values
    - statement: "MUST NOT add a loading state, an error state, or a retry affordance to the activity feed. It has none today, and 03-UI-SPEC.md flags the absence of error UI as a deliberate, out-of-scope gap."
      category: values
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or marking-todo any existing test, and MUST NOT ship a vacuous new one."
      category: transparency
    - statement: "MUST NOT perform a bulk `sed`, codemod, or editor-wide find-and-replace across `mobile/src`. Per-file edits only."
      category: safety
  artifacts:
    - path: "mobile/src/components/FriendsScreen/ActivityRow.jsx"
      provides: "One activity-feed row (FRIENDS-03); render tier, zero callback props"
      contains: "export default function ActivityRow"
      min_lines: 22
    - path: "mobile/src/components/FriendsScreen/__tests__/ActivityRow.test.jsx"
      provides: "Render coverage for the feed row per D-01"
      contains: "render"
      min_lines: 20
  key_links:
    - from: "mobile/src/components/FriendsScreen/ActivityTab.jsx"
      to: "mobile/src/components/FriendsScreen/ActivityRow.jsx"
      via: "the container derives icon, colour, actor, timestamp and text per item and hands them to the row"
      pattern: "ActivityRow"
---

<objective>
Split the one repeated row out of the Activity tab container into its own named component, leaving every derivation where it already is.

**As a** student scrolling my activity feed, **I want to** see the same colour-coded icon, the same attribution and the same wording on every row, **so that** splitting the row into its own file is invisible to me.

Purpose: the Activity container was extracted whole in plan 03-01 with its row markup inline. This plan completes it into named widgets, and it is the only widget split that can run this early — the container it splits already exists after wave 1, and it shares no file with anything else in wave 2.

Output: `ActivityRow.jsx`, its render test, and an `ActivityTab` whose map body is one component render.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-friendsscreen-decomposition/03-CONTEXT.md
@.planning/phases/03-friendsscreen-decomposition/03-UI-SPEC.md
@.planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md
@.planning/phases/03-friendsscreen-decomposition/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files created by **Phase 3** (this plan's contributions marked ★). Every path is git-tracked source.

| Symbol / file | Kind | Path | Plan |
|---|---|---|---|
| `useFriendsScreenState(navigation, route)` | named export, hook | `mobile/src/hooks/useFriendsScreenState.js` | 03-01 |
| `EmptyState`, `ActivityTab` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-01 |
| `MEDALS` | named export, const array | `mobile/src/components/FriendsScreen/medals.js` | 03-02 |
| `Avatar`, `Podium`, `RankRow` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-02 |
| `LeaderboardTab` | default export, component | `mobile/src/components/FriendsScreen/LeaderboardTab.jsx` | 03-03 |
| `FriendsTab`, `BattlesTab` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-04 |
| `FriendRequestBanner`, `IncomingBattleBanner`, `FriendsTabBar` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-05 |
| ★ `ActivityRow` | default export, component | `mobile/src/components/FriendsScreen/ActivityRow.jsx` | 03-06 |
| `LeaderboardModeToggle`, `FriendsLeaderboard` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-07 |
| `SchoolLeaderboard`, `SchoolRankRow`, `SchoolPickerPrompt`, `LeagueLeaderboard`, `LeagueRankRow` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-08 |
| `FriendCodeBanner`, `FriendCard` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-09 |
| `PendingBattleRow`, `CompletedBattleRow` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-10 |
| `mobile/src/__tests__/useFriendsScreenState.test.js` | test file | — | 03-01 |
| ★ 1 of 23 `*.test.jsx` files | test files | `mobile/src/components/FriendsScreen/__tests__/` | 03-06 |
| `03-CHARACTERIZATION.md`, `03-EXTRACTION-NOTES.md` | docs | `.planning/phases/03-friendsscreen-decomposition/` | 03-01, 03-11 |

Removed from `mobile/src/components/FriendsScreen/ActivityTab.jsx` by ★ this plan: the inline feed-row markup and the three feed-row style keys, which move into the row component.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract ActivityRow as a render-tier leaf</name>

  <files>mobile/src/components/FriendsScreen/ActivityRow.jsx, mobile/src/components/FriendsScreen/__tests__/ActivityRow.test.jsx</files>

  <read_first>
    - `mobile/src/components/FriendsScreen/ActivityTab.jsx` — the whole file as plan 03-01 created it. The block inside the per-item map, from the row wrapper down, is what moves.
    - `mobile/src/components/FriendsScreen/ActivityTab.jsx` — its local style factory. The feed-row, feed-icon-wrapper and feed-actor keys move into the new file; the list-wrapper key stays.
    - `mobile/src/components/FocusScreen/SessionSummaryStats.jsx` and its test — this repo's precedent for a render-tier leaf with no callback props.
    - `.planning/phases/03-friendsscreen-decomposition/03-UI-SPEC.md` sections "Color" and "Visual Hierarchy" — the eight per-type accent colours and the note that the colour-coded icon is the first-glance differentiator down the feed.
  </read_first>

  <behavior>
    Assertions `ActivityRow.test.jsx` must make. This component has zero callback props, so per **D-01** it
    is render tier: assert visible output only, never simulate an interaction.

    - Renders the icon glyph, the actor string, the relative-time string and the body text it is given.
    - Renders the icon circle tinted from the colour it is given, asserted through the rendered style.
    - With the self flag set, renders the actor in the brand colour; without it, in the default text colour.
  </behavior>

  <action>
    Create `mobile/src/components/FriendsScreen/ActivityRow.jsx`. Default-exported component. Move the
    per-item row markup across byte-for-byte from inside the container's map: the row wrapper with its card
    shadow, the tinted icon circle whose background and border are derived from the colour by the same
    opacity suffixes used today, the actor-and-time line with its exact gap and bottom margin, and the body
    text with its exact line height.

    Read the theme inside the component and give the file its own local style factory holding exactly the
    three keys this row uses — the row, the icon wrapper and the actor text — each copied
    character-for-character from the container's factory. Import the typography scale and the card-shadow
    helper from the shared styles module, both already used here. Do not import the button, card, pill or
    section-label factories.

    Props, all plain values computed by the container: the icon glyph, the accent colour, the actor string,
    a boolean for whether the actor is the current user, the relative-time string, and the body text. No
    callbacks. Do not move any derivation into this component — the lookup, the attribution chain and the
    timestamp computation all stay in the container, so that this leaf is a pure function of its props and
    the derivation keeps exactly one home.

    Write `ActivityRow.test.jsx` under the tests directory. Mock the theme context three levels up from the
    tests directory. Await `render()` and query by the visible strings. Write the file's head comment so
    that it describes the render-only tier **without naming the interaction-helper function the phase's tier
    gate greps for** — Phase 2's Trap 5 is exactly this.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FriendsScreen/__tests__/ActivityRow.test.jsx 2>&1 | tail -20</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the output contains `No tests found`.</fails_when>
    <automated>grep -cE 'onPress=|onLongPress=|onChangeText=|onSubmitEditing=|onRequestClose=' mobile/src/components/FriendsScreen/ActivityRow.jsx</automated>
    <fails_when>the printed count is greater than 0 — the feed row is render tier and must declare no interactive handler attribute.</fails_when>
    <automated>grep -c -e 'rp_earned' -e 'level_up' -e 'quiz_complete' -e 'timeAgo' mobile/src/components/FriendsScreen/ActivityRow.jsx</automated>
    <fails_when>the printed count is greater than 0 — derivation leaked into the leaf; the type lookup and the timestamp computation belong to the container.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FriendsScreen/ActivityRow.jsx` exists with a default export and its own three-key local style factory.
    - The component is a pure function of its six props with no lookup table and no timestamp arithmetic.
    - `grep -c '@react-navigation' mobile/src/components/FriendsScreen/ActivityRow.jsx` prints 0.
    - The test simulates no interaction and asserts the self-versus-other actor colour through the rendered style.
    - `npm --prefix mobile test -- src/components/FriendsScreen/__tests__/ActivityRow.test.jsx` passes.
  </acceptance_criteria>

  <done>One feed row renders from a named leaf with all derivation left in its container, covered at render tier per D-01.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire ActivityTab to render the row component</name>

  <files>mobile/src/components/FriendsScreen/ActivityTab.jsx, mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx</files>

  <read_first>
    - `mobile/src/components/FriendsScreen/ActivityTab.jsx` — the whole file, so you replace only the markup inside the map and keep every derivation line above it untouched.
    - `mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx` — the existing assertions from plan 03-01. They must all still pass unchanged; this split is not allowed to require rewriting them.
    - `mobile/src/components/FriendsScreen/ActivityRow.jsx` — the six-prop signature you just authored.
  </read_first>

  <action>
    In `mobile/src/components/FriendsScreen/ActivityTab.jsx`, replace the markup inside the per-item map with
    a single render of the row component, passing the six values the surrounding derivation already computes.
    Keep every line of that derivation exactly where it is: the type lookup table defined inside the callback,
    the icon-and-colour destructure with its fallback pair, the self check, the friend lookup, the actor
    fallback chain, and the timestamp computation with both of its branches and its default. Keep the React
    key on the rendered row exactly as it is today.

    Remove from the container's local style factory the three keys that moved into the row component. Keep
    the list-wrapper key. Remove the card-shadow import if the container no longer uses it.

    Do not modify `ActivityTab.test.jsx` except to add assertions. Its existing assertions from plan 03-01 are
    the regression net for this split: if one of them now fails, the split changed behavior and the split is
    wrong, not the test.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FriendsScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test — in particular a previously-passing assertion in the container's own test file, which would mean the split changed behavior.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>grep -c -e 'rp_earned' -e 'focus_session_complete' -e 'A friend' -e 'just now' mobile/src/components/FriendsScreen/ActivityTab.jsx</automated>
    <fails_when>the printed count is less than 4 — a piece of the per-item derivation left the container when it was supposed to stay.</fails_when>
    <automated>grep -c 'feedIconWrap' mobile/src/components/FriendsScreen/ActivityTab.jsx</automated>
    <fails_when>the printed count is greater than 0 — a style key that moved into the row component is still declared in the container.</fails_when>
  </verify>

  <acceptance_criteria>
    - `ActivityTab.jsx`'s map body is a single row render plus the surrounding derivation.
    - The container still contains all eight activity type keys, the fallback pair, the actor fallback chain and the timestamp default.
    - The container's style factory holds only the list-wrapper key.
    - Every assertion `ActivityTab.test.jsx` carried before this plan still passes without modification.
    - `npm --prefix mobile run check` exits 0.
    - `git status --porcelain chromebook src functions shared` prints nothing — mobile-only change set.
  </acceptance_criteria>

  <done>The Activity tab composes a named row component while keeping its derivation in one place, and its pre-existing tests still pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly introduced | This plan moves presentational client-side code between two files under `mobile/src/components/FriendsScreen/`. No network call, no persisted data, no auth logic, and no user-input path is added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Spoofing | actor attribution in the feed row | low | mitigate | Mis-splitting the attribution chain could label another user's activity with the self-word, or vice versa. The chain stays entirely in the container and the leaf receives an already-resolved string plus a boolean; enforced by the derivation-leak grep gates in both tasks and by the self-versus-other colour assertion. |
| T-03-14 | Information disclosure | feed body text rendered in the row | low | accept | The row renders the activity text the domain hook already supplies, exactly as today. What is written into the feed and who may read it are governed by unchanged Firestore rules. |
| T-03-SC | Tampering | package-manager installs | low | accept | No install task exists in this plan. `03-RESEARCH.md` records the Package Legitimacy Audit as not applicable: zero new external packages. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0.
- Every assertion `ActivityTab.test.jsx` carried before this plan still passes without modification.
- `git status --porcelain chromebook src functions shared` is empty — mobile-only change set.
</verification>

<success_criteria>
- FRIENDS-03 advanced: one more named sub-component exists under `components/FriendsScreen/`.
- FRIENDS-05 satisfied for `ActivityRow`: it has zero callback props, so it has a render-tier test per D-01.
</success_criteria>

<output>
Create `.planning/phases/03-friendsscreen-decomposition/03-06-SUMMARY.md` when done. Record: the row component's final prop list; confirmation that no derivation moved out of the container; and any discrepancy between 03-UI-SPEC.md's transcription of the feed row and the actual source — as a finding, not a fix.
</output>
