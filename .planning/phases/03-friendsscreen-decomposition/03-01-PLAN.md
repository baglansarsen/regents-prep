---
phase: 03-friendsscreen-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md
  - mobile/src/hooks/useFriendsScreenState.js
  - mobile/src/screens/FriendsScreen.jsx
  - mobile/src/components/FriendsScreen/EmptyState.jsx
  - mobile/src/components/FriendsScreen/ActivityTab.jsx
  - mobile/src/__tests__/useFriendsScreenState.test.js
  - mobile/src/components/FriendsScreen/__tests__/EmptyState.test.jsx
  - mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx
autonomous: true
requirements: [FRIENDS-01, FRIENDS-02, FRIENDS-03, FRIENDS-04, FRIENDS-05]

estimate:
  tokens: 88000
  raw_tokens: 88000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md` exists and is committed BEFORE any line of `mobile/src/screens/FriendsScreen.jsx` changes — the FRIENDS-06 parity baseline is worthless if it is written after the code moved"
    - "The characterization notes state, in their own words, that no friend-request *block* feature exists anywhere in `mobile/src/screens/FriendsScreen.jsx` or `mobile/src/hooks/useFriends.js`, and that the three real request-adjacent behaviors are accept, decline, and remove-an-existing-friend via long-press (03-RESEARCH.md Pitfall 4 — FRIENDS-01 inherited stale wording from `.planning/codebase/CONCERNS.md`)"
    - "Writing the characterization notes is a single pre-extraction act: they are committed once, before the first source commit, and are not amended, re-derived, or re-run against the post-extraction code at any later point in this phase (edge probe: FRIENDS-01 idempotency)"
    - "If this phase is interrupted after the baseline commit and resumed later, the baseline's independence still holds, because independence is established by commit order alone and not by any in-flight session state (edge probe: FRIENDS-01 concurrency)"
    - "`mobile/src/screens/FriendsScreen.jsx` calls exactly one orchestration hook — `useFriendsScreenState(navigation, route)` — and declares no local React state, no effects, and no `Alert` call of its own (D-01, D-07)"
    - "`useFriendsScreenState` calls all five domain hooks — `useFriends`, `useChallenges`, `useLeaderboard`, `useFriendsLeaderboard`, `useLeague` — and all five of `mobile/src/hooks/useFriends.js`, `useChallenges.js`, `useLeaderboard.js`, `useFriendsLeaderboard.js`, `useLeague.js` are byte-identical before and after this phase (wrap, don't absorb)"
    - "`useFriendsScreenState` preserves the exact destructure-rename pairs the shipped screen already uses — `loading: schoolLoading`, `refresh: refreshSchool`, `loading: weekLoading`, `refresh: refreshWeekly`, `refresh: refreshLeague`, `refresh: refreshBattles`, `incoming: incomingBattles`, `completed: completedBattles`, `members: leagueMembers` — and invents no new name for any of them (D-03, locked)"
    - "`useFriendsScreenState` returns one flat object: every field sits at the top level, and no domain hook's return object is spread wholesale (D-02 — a wholesale spread would silently reintroduce the `loading`/`refresh` collision D-03 exists to prevent)"
    - "A fresh mount with no route params returns `tab === 'Leaderboard'`, `lbMode === 'friends'`, `podiumKey === 0`, `refreshing === false`, and empty arrays for `friends`, `incomingRequests`, `sentRequests`, `feed`, `weeklyRanking`, `leaderboard`, `leagueMembers`, `incomingBattles`, `completedBattles` (edge probe: FRIENDS-04 empty)"
    - "A fresh mount with `route.params.initialTab` set to `'Battles'` returns `tab === 'Battles'` — the existing `route?.params?.initialTab ?? 'Leaderboard'` contract survives the move into the hook (D-07)"
    - "`top3` is `weeklyRanking.slice(0, 3)` and `restList` is `weeklyRanking.slice(3)`, so a ranking of exactly 3 entries yields a 3-entry `top3` and an empty `restList`, and a ranking of exactly 4 yields a 3-entry `top3` and a 1-entry `restList` (edge probe: FRIENDS-04 adjacency at the 3/4 boundary)"
    - "`top3` and `restList` preserve `weeklyRanking`'s incoming order exactly; the hook performs no sort, reverse, or stable-tie-break of its own (edge probe: FRIENDS-04 ordering — this is a parity-lock extraction, so the correct behavior is whatever the pre-extraction screen did, verbatim)"
    - "`handleRefresh()` awaits all seven refreshes — `refreshWeekly`, `refreshSchool`, `refreshFeed`, `refreshRequests`, `refreshFriends`, `refreshBattles`, `refreshLeague` — in one `Promise.all`, then increments `podiumKey` by exactly 1, then clears `refreshing`; the order of those three steps is unchanged (D-01)"
    - "`confirmRemoveFriend(f)` shows a two-button `Alert.alert` whose second button is styled destructive and calls `removeFriend(f.id ?? f.uid)`; pressing the first button removes nobody (D-01)"
    - "Every navigation target the screen reaches today is reachable through a named handler returned by the hook, and no component under `mobile/src/components/FriendsScreen/` ever receives a routing object as a prop"
    - "`mobile/src/components/FriendsScreen/ActivityTab.jsx` renders the activity feed exactly as the shipped screen's Activity tab rendered it: the same per-type icon/colour map, the same `You` / friend-display-name / `A friend` attribution fallback chain, the same `just now` timestamp default, and the same `Activity` text fallback (03-UI-SPEC.md Copywriting Contract)"
    - "With an empty `feed`, `ActivityTab` renders the `No Activity Yet` empty-state block and zero feed rows (UI-SPEC UI Considerations, `empty` / Activity tab)"
    - "`ActivityTab` renders feed rows in the order of the `feed` array it is given, adding no sort of its own (edge probe: FRIENDS-05 ordering)"
    - "`npm --prefix mobile run check` exits 0 — the full jest suite plus the babel parse-check of changed src files"
  prohibitions:
    - statement: "MUST NOT change any user-visible behavior or pixel while claiming a structure-only extraction. A bug, inconsistency, or obvious improvement discovered during the move is recorded in the plan SUMMARY as a finding and left exactly as it is — PROJECT.md is explicit that a behavior change discovered as needed is a signal to stop and flag, not to slip in."
      category: values
    - statement: "MUST NOT add a loading state, an error state, a retry affordance, or empty-state copy that the shipped screen does not already have. 03-UI-SPEC.md flags the plain-text loading copy and the total absence of error UI as two deliberate, out-of-scope gaps; inventing handling for either is a behavior change disguised as polish."
      category: values
    - statement: "MUST NOT rename, merge, or tidy the destructure-rename pairs the screen uses today for the leaderboard/league/battle loading and refresh fields. Those names are the existing resolution of a real collision, not a style choice."
      category: values
    - statement: "MUST NOT write or amend the characterization notes after extraction has begun, and MUST NOT adjust them to match what the new code does. Their only value is as an independent pre-extraction baseline; a retro-fitted baseline turns FRIENDS-06 into a tautology."
      category: transparency
    - statement: "MUST NOT reach a green suite by deleting, skipping, weakening, or marking-todo any existing test, and MUST NOT ship a vacuous new one. A render assertion that only proves no exception was thrown certifies nothing while looking like proof."
      category: transparency
    - statement: "MUST NOT promote the initial-letter avatar into a shared component or edit `mobile/src/screens/LeagueScreen.jsx`. The byte-for-byte duplication between the two screens is a logged, deliberate deferral (D-06); unifying it here silently widens the blast radius beyond this phase's four-screen scope."
      category: values
    - statement: "MUST NOT perform a bulk `sed`, codemod, or editor-wide find-and-replace across `mobile/src`. Per-file edits only — a past bulk rename shipped a crashing TestFlight build, which is why the repo instructions forbid it by name."
      category: safety
  artifacts:
    - path: ".planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md"
      provides: "Pre-extraction behavior baseline for FRIENDS-01 and the reference the FRIENDS-06 manual parity pass is scored against"
      contains: "Requirement-Text Correction"
      min_lines: 80
    - path: "mobile/src/hooks/useFriendsScreenState.js"
      provides: "The single orchestration hook FriendsScreen calls (FRIENDS-02); wraps all five domain hooks and owns the screen's former state, effects, and handlers"
      contains: "useFriendsLeaderboard"
      min_lines: 140
    - path: "mobile/src/screens/FriendsScreen.jsx"
      provides: "Screen reduced toward a pure renderer: one hook call plus shell chrome"
      contains: "useFriendsScreenState"
    - path: "mobile/src/components/FriendsScreen/EmptyState.jsx"
      provides: "The icon + heading + body empty-state block used identically by four surfaces in this screen"
      contains: "export default function EmptyState"
      min_lines: 12
    - path: "mobile/src/components/FriendsScreen/ActivityTab.jsx"
      provides: "Activity tab container (FRIENDS-03), first tab container under components/FriendsScreen/"
      contains: "No Activity Yet"
      min_lines: 55
    - path: "mobile/src/__tests__/useFriendsScreenState.test.js"
      provides: "Hook state-transition coverage for FRIENDS-04"
      contains: "renderHook"
      min_lines: 120
    - path: "mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx"
      provides: "Render coverage for the Activity container per D-01 (zero callback props, so render tier)"
      contains: "render"
      min_lines: 30
    - path: "mobile/src/components/FriendsScreen/__tests__/EmptyState.test.jsx"
      provides: "Render coverage for the shared empty-state leaf per D-01"
      contains: "render"
      min_lines: 12
  key_links:
    - from: "mobile/src/screens/FriendsScreen.jsx"
      to: "mobile/src/hooks/useFriendsScreenState.js"
      via: "the screen's only orchestration hook call; everything it renders comes from this return value"
      pattern: "useFriendsScreenState\\(navigation, route\\)"
    - from: "mobile/src/hooks/useFriendsScreenState.js"
      to: "mobile/src/hooks/useFriends.js"
      via: "wrap-don't-absorb composition — the new hook calls the existing domain hook, which is never edited"
      pattern: "useFriends\\(uid, user\\)"
    - from: "mobile/src/hooks/useFriendsScreenState.js"
      to: "mobile/src/hooks/useLeague.js"
      via: "tier metadata lookup reused unchanged"
      pattern: "TIER_META"
    - from: "mobile/src/components/FriendsScreen/ActivityTab.jsx"
      to: "mobile/src/components/FriendsScreen/EmptyState.jsx"
      via: "the zero-feed branch renders the shared empty-state leaf"
      pattern: "EmptyState"
---

<objective>
Stand up the FriendsScreen extraction skeleton end to end: write the pre-extraction behavior baseline, create the `useFriendsScreenState` orchestration hook that wraps all five existing domain hooks, reduce `mobile/src/screens/FriendsScreen.jsx` to a single hook call plus shell chrome, and prove the whole path works by moving the smallest tab branch (Activity) into the first real components under `mobile/src/components/FriendsScreen/` — with hook state-transition tests and component render tests alongside.

**As a** developer validating Phase 2's extraction template on a second, harder screen, **I want to** see one complete slice of FriendsScreen run screen → orchestration hook → five untouched domain hooks → named sub-component → tests, **so that** the remaining three tab branches and every widget split copy a contract that is already proven against this screen rather than one carried over on paper from a screen with one domain hook instead of five.

Purpose: FRIENDS-02's single-hook requirement and FRIENDS-03's named-sub-component requirement are architectural. If the hook's flat return contract is wrong, all ten later plans in this phase inherit the mistake. This plan fixes the contract first and validates it against real render output.

Output: `03-CHARACTERIZATION.md`, `useFriendsScreenState.js`, a thinner `FriendsScreen.jsx`, `EmptyState.jsx`, `ActivityTab.jsx`, and three test files.

**This is the tracer slice.** It wires one path through every layer this phase touches — characterization doc → orchestration hook → the five untouched domain hooks → screen → named components in the new directory → hook test → component tests → green `npm --prefix mobile run check`. It is production-quality, not a throwaway: nothing here is rewritten by a later plan, only expanded outward from.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-friendsscreen-decomposition/03-CONTEXT.md
@.planning/phases/03-friendsscreen-decomposition/03-RESEARCH.md
@.planning/phases/03-friendsscreen-decomposition/03-PATTERNS.md
@.planning/phases/03-friendsscreen-decomposition/03-UI-SPEC.md
@.planning/phases/02-focusscreen-decomposition/02-EXTRACTION-TEMPLATE.md
</context>

<planning_notes>
**Capability checkpoints, run this planning session:**

- API-coverage gate: this phase integrates zero new external APIs — it relocates existing calls already made by five unmodified domain hooks. Skipped, no `COVERAGE.md`.
- Assumption-delta checkpoint: no singular→plural / required→optional / derived→chosen transition in scope. Skipped.
- Schema-push gate: no ORM/schema files (Prisma, Drizzle, Supabase, TypeORM, Payload) anywhere in this phase's file set. Skipped, no `[BLOCKING]` task.
- Package-legitimacy gate: no package-manager install task exists in this phase; `03-RESEARCH.md` records the audit as not applicable — zero new external packages.
- Reversibility gates: no decision in `03-CONTEXT.md` rates `one-way`, so no `checkpoint:decision` is inserted. D-03 is rated `costly` by the discuss phase and is carried forward as a `<reversibility>` element on Task 2.
- `workflow.human_verify_mode` is `end-of-phase`, so this phase emits no `checkpoint:human-verify` task. FRIENDS-06's manual pass is carried as a `<verify><human-check>` in plan 03-11 and harvested into `03-UAT.md`.

**ROADMAP goal is not in user-story form.** Phase 3's `**Goal:**` line in `.planning/ROADMAP.md` is written as an outcome, not as "As a / I want to / so that". The story in `<objective>` above is a restatement of that same goal with the developer as the user, following the precedent `.planning/phases/02-focusscreen-decomposition/02-01-PLAN.md` set for the identically-shaped Phase 2 goal. Flagged here rather than silently invented: if you want a first-class user story on the roadmap line, run `/gsd mvp-phase 3` before executing.

**Planner deviations from `03-RESEARCH.md`, deliberate:**

1. **Eleven plans across six waves, not RESEARCH's single batched wave-2 plan.** RESEARCH proposes one plan that creates the hook, reduces the screen, relocates three components, and extracts all four tab containers. Read against the actual file, that is roughly 470 lines of relocated JSX plus a 5-hook orchestration hook plus seven test files in one plan — far past the smart-zone budget for work whose whole value is byte-level fidelity. Split instead: one screen-editing plan per wave (the shared-file rule forces those sequential anyway), with widget-split plans run in parallel beside them on disjoint files. Wave count matches RESEARCH's estimate of 6; plan count is higher because each plan is smaller.
2. **An eighth navigation call site exists that RESEARCH did not list.** RESEARCH names lines 332, 455, 519, 593, 645, 687 and 721. `mobile/src/screens/FriendsScreen.jsx` line 561 is a ninth navigate call overall (`League`, from the footer link below the league standings). Counted and handled here.
3. **Two of the battle-challenge call sites are verifiably identical and share one handler.** The shell banner and the Battles-tab pending row both build their parameters from the same `incomingBattles` record with the same two keys. Phase 2's Trap 2 says read both at source level before assuming mergeability — both were read this session and they *are* identical, so one handler serves both. The other two challenge call sites take a different parameter key and get their own handlers.
4. **One shared empty-state leaf, not four copies.** Four surfaces render a byte-identical icon/heading/body block. Phase 2's judgment rule is to keep components separate when the *behavior* differs; here nothing differs, so this is the case where sharing is correct. Created in this plan because the tracer's own container needs it immediately, which also means every later plan only imports it and no wave is forced sequential by it.
5. **The screen keeps its own theme read.** FocusScreen ended at 13 lines with zero context reads because its containers owned all chrome. FriendsScreen's shell keeps four always-visible pieces above the tab content, so it keeps a minimal local style object and a theme read. The FRIENDS-02 gate is therefore construct-based (no local state, no effects, no alerts, no direct navigate calls, exactly one orchestration hook call) rather than a line ceiling; plan 03-05 closes it.

**Edge-coverage disposition (spec-less fallback, 11 probe rows, zero silent drops).** No SPEC exists for this phase, so the probe ran over FRIENDS-01…06's requirement text. Every row is dispositioned; nothing is dropped:

| Req | Probe category | Disposition |
|-----|----------------|-------------|
| FRIENDS-01 | idempotency | Resolved (explicit) — baseline written once, never amended. Truth #3, this plan. |
| FRIENDS-01 | concurrency | Resolved (explicit) — independence rests on commit order alone. Truth #4, this plan. |
| FRIENDS-02 | unclassified | **Stays unresolved**, surfaced as a flagged assumption: the probe taxonomy is shape-based and FRIENDS-02 is a structural requirement with no data-shape axis. Its substance is still carried as the single-hook-call truth here and the construct gate in plan 03-05. |
| FRIENDS-03 | unclassified | **Stays unresolved**, flagged for the same reason. Substance carried as the per-plan artifact lists. |
| FRIENDS-04 | adjacency | Resolved (explicit) — the `top3`/`restList` split at exactly 3 and 4 entries. Truth #11, this plan. |
| FRIENDS-04 | empty | Resolved (explicit) — fresh-mount return shape. Truth #9, this plan. |
| FRIENDS-04 | ordering | Resolved (explicit) — no sort of the hook's own; parity means whatever shipped. Truth #12, this plan. |
| FRIENDS-05 | adjacency | Resolved (explicit) in plans 03-05 and 03-07 — active state for both the tab bar and the leaderboard mode segment is decided by exact string equality. |
| FRIENDS-05 | empty | Resolved (explicit) — each container's zero-item branch. Truth #17 here for Activity; the same truth appears in 03-04, 03-07 and 03-08 for the other surfaces. |
| FRIENDS-05 | ordering | Resolved (explicit) — list components render in the order of the array given. Truth #18 here; repeated per list container. |
| FRIENDS-06 | unclassified | **Stays unresolved**, flagged: a manual parity pass has no data-shape axis. Substance carried as the `<human-check>` in plan 03-11. |

**UI-SPEC `## UI Considerations` lift (12 rows, zero silent drops).** Ten `✅ covered` rows lift to explicit truths: Activity-empty → this plan; Friends-tab-empty and Battles-tab-empty and sent-requests zero-one-many → 03-04; Friends-mode-leaderboard-empty → 03-07; school-no-school and school-solo and league-empty → 03-08; Podium zero-one-many → 03-02; name-overflow single-line truncation → 03-02, 03-08 and 03-09. The two `⚠ unresolved` rows are recorded here as explicit planner assumptions: **the plain-text loading copy and the complete absence of error UI are both pre-existing gaps in the shipped screen, and this phase adds handling for neither** — surfaced so no executor reads their absence as an omission to fix. Both are additionally bound by this plan's second prohibition, which every plan in the phase carries.

**Prohibition recall (Stage 1 adversarial → Stage 2 precision → canon referral).** Stage 1 over-produced twelve candidates; Stage 2 dropped the routine-engineering ones (no input mutation, no throw on empty feed, no leaked timer — all owned by the edge probe or ordinary review) and kept seven values/safety/transparency items, authored descriptor-less above. **Canon-referral breadcrumb:** OWASP, injection, path-traversal and prototype-pollution candidates are canon — covered by `/gsd-secure-phase` and lint tooling, and inapplicable to a client-side structural refactor that adds no trust boundary. Not minted here.
</planning_notes>

<artifacts_this_phase_produces>
Symbols and files created by **Phase 3** (this plan's contributions marked ★). Every path below is git-tracked source under `mobile/src` or `.planning/`; none is an install or runtime mirror.

| Symbol / file | Kind | Path | Plan |
|---|---|---|---|
| ★ `useFriendsScreenState(navigation, route)` | named export, hook | `mobile/src/hooks/useFriendsScreenState.js` | 03-01 |
| ★ `EmptyState` | default export, component | `mobile/src/components/FriendsScreen/EmptyState.jsx` | 03-01 |
| ★ `ActivityTab` | default export, component | `mobile/src/components/FriendsScreen/ActivityTab.jsx` | 03-01 |
| `Avatar` | default export, component | `mobile/src/components/FriendsScreen/Avatar.jsx` | 03-02 |
| `Podium` | default export, component | `mobile/src/components/FriendsScreen/Podium.jsx` | 03-02 |
| `RankRow` | default export, component | `mobile/src/components/FriendsScreen/RankRow.jsx` | 03-02 |
| `MEDALS` | named export, const array | `mobile/src/components/FriendsScreen/medals.js` | 03-02 |
| `LeaderboardTab` | default export, component | `mobile/src/components/FriendsScreen/LeaderboardTab.jsx` | 03-03 |
| `FriendsTab`, `BattlesTab` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-04 |
| `FriendRequestBanner`, `IncomingBattleBanner`, `FriendsTabBar` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-05 |
| `ActivityRow` | default export, component | `mobile/src/components/FriendsScreen/ActivityRow.jsx` | 03-06 |
| `LeaderboardModeToggle`, `FriendsLeaderboard` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-07 |
| `SchoolLeaderboard`, `SchoolRankRow`, `SchoolPickerPrompt`, `LeagueLeaderboard`, `LeagueRankRow` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-08 |
| `FriendCodeBanner`, `FriendCard` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-09 |
| `PendingBattleRow`, `CompletedBattleRow` | default exports, components | `mobile/src/components/FriendsScreen/` | 03-10 |
| ★ `mobile/src/__tests__/useFriendsScreenState.test.js` | test file | — | 03-01 |
| ★ 2 of 23 `*.test.jsx` files | test files | `mobile/src/components/FriendsScreen/__tests__/` | 03-01 |
| 21 further `*.test.jsx` files | test files | `mobile/src/components/FriendsScreen/__tests__/` | 03-02 … 03-10 |
| ★ `03-CHARACTERIZATION.md` | doc | `.planning/phases/03-friendsscreen-decomposition/` | 03-01 |
| `03-EXTRACTION-NOTES.md` | doc | `.planning/phases/03-friendsscreen-decomposition/` | 03-11 |

Removed by this phase from `mobile/src/screens/FriendsScreen.jsx`: the module-local `Avatar`, `Podium` and `RankRow` function declarations, the `TABS`, `MEDALS`, `BAR_HEIGHTS` and `REVEAL_ORDER` constants, and the `makeStyles` factory (sliced across the extracted components, each key copied byte-for-byte into the one component that uses it).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Record the pre-extraction behavior baseline (FRIENDS-01)</name>

  <precondition>The npm registry is reachable — `npm ping` exits 0 — or `mobile/node_modules` is already populated. `mobile/node_modules` is gitignored and may be absent from a fresh worktree, so the suite cannot run until an install completes.</precondition>

  <files>.planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md</files>

  <read_first>
    - `mobile/src/screens/FriendsScreen.jsx` — read the whole 987-line file. You are documenting what it does today; every claim in the notes must come from this read, not from the requirement text.
    - `mobile/src/hooks/useFriends.js` — owns friends, incoming and sent requests, the friend code, the activity feed, and the accept/decline/remove writes.
    - `mobile/src/hooks/useChallenges.js`, `mobile/src/hooks/useLeaderboard.js`, `mobile/src/hooks/useFriendsLeaderboard.js`, `mobile/src/hooks/useLeague.js` — read at least each file's return statement, so the notes describe the data the screen actually receives.
    - `.planning/phases/03-friendsscreen-decomposition/03-UI-SPEC.md` sections "Copywriting Contract" and "UI Considerations" — the visual half of the parity baseline already exists there. Reference it; do not duplicate it.
    - `.planning/phases/03-friendsscreen-decomposition/03-RESEARCH.md` section "Pitfall 4" — explains why FRIENDS-01's wording does not match the code.
    - `.planning/phases/02-focusscreen-decomposition/02-CHARACTERIZATION.md` — the format precedent this document follows.
  </read_first>

  <action>
    Create `.planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md`. Depth target is
    "enough for a human to score FRIENDS-06 parity against", not exhaustive prose — per 03-CONTEXT.md's
    Claude's-Discretion note on characterization format.

    Required sections, in this order, each as a level-2 heading:

    1. `## Requirement-Text Correction` — state plainly that FRIENDS-01's words describe a friend-request
       *block* capability that does not exist in this screen. Record the evidence: a whole-file read plus
       a case-insensitive grep for that word across `mobile/src/hooks/useFriends.js` and
       `mobile/src/screens/FriendsScreen.jsx`, which returns nothing. Name `.planning/codebase/CONCERNS.md`
       as the origin of the inherited wording, and note that Phase 2 hit the identical class of mismatch.
       State that the real analogs documented below are accept, decline, and remove-an-existing-friend,
       and that no such capability is to be built.

    2. `## Four-tab structure` — the four tabs in render order with their emoji and label, the fact that
       the initial tab comes from the route parameter and otherwise defaults to the leaderboard tab,
       that exactly one tab body renders at a time, and that switching tabs neither refetches nor resets
       any other tab's state.

    3. `## Always-visible chrome` — the four pieces that render above the tab body regardless of tab:
       the screen header, the pending-friend-request banner (only when at least one request is pending),
       the incoming-battle banner (only when at least one challenge is pending), and the tab bar. Record
       that the two banners are notification-level and deliberately sit outside the tab they logically
       belong to.

    4. `## Leaderboard segment toggle` — the three-way segment and its exact labels, that the default is
       the friends segment, that active state is decided by exact string equality against the current
       mode, that the league segment's emoji comes from the tier metadata, and that switching segments
       neither refetches nor resets. Then one short subsection per segment describing every branch it can
       render: friends (loading text, no-ranking message, the distinct add-friends message, podium plus
       rank list, the resets footer), school (loading text, no-school prompt with its call to action,
       the solo-student message keyed off a list length of one or fewer, the ranked list with medals for
       the first three, the resets footer), and league (the tier banner, the no-members message, the
       ranked list, the promotion-zones footer link). Record that only the friends segment substitutes
       the word for oneself into the name text, and that the league segment does not, although both
       colour the current user's name with the brand colour.

    5. `## Friend requests and friendships` — the three real behaviors. Accept and decline from the
       always-visible banner, each an icon-only control. Remove an existing friend by long-pressing a
       friend card, which opens a two-button confirmation whose second button is destructive. Record the
       exact confirmation title and message including the fallback wording when a display name is absent.
       Record the sent-requests line and that it renders only when at least one request is outstanding,
       with names comma-joined and no singular/plural variation.

    6. `## Battles` — the incoming-challenge banner, the Battles tab's own pending section and results
       section, that each section's heading renders only when its own list is non-empty, that the
       combined-empty case shows the empty-state block, and the exact outcome wording and score format
       on a completed row. Record that a pending row and a completed row are visibly different shapes:
       one is pressable with a play control, the other is a static row with an outcome badge and a
       rematch control.

    7. `## Activity feed` — the per-type icon and colour map with all eight types plus the fallback pair,
       the attribution chain (self, then a matching friend's display name, then the generic fallback),
       the timestamp derivation including its default when no timestamp is present, and the text fallback.

    8. `## Pull to refresh` — that a pull refreshes seven data sources at once, then forces the podium to
       remount so its reveal animation replays, then clears the refreshing flag — in that order.

    9. `## Animations` — the podium's four-phase staggered reveal with its exact delays and the reveal
       order, the rank row's per-index slide-in delay formula, and that both clear their pending timers
       on unmount.

    10. `## Not present in this screen` — one line each for: no block capability, no error or failure UI
        anywhere, no spinner (the two loading states are plain text), no retry affordance, no search, and
        no filter control. Cross-reference the two `⚠ unresolved` rows in 03-UI-SPEC.md's UI Considerations
        table and state that this phase adds none of them.

    11. `## How to score FRIENDS-06` — a short numbered checklist a human can walk in the running app,
        one line per section above.

    Commit this file on its own, before touching any source file. The whole point of a characterization
    baseline is that it was written without knowledge of the new implementation.
  </action>

  <verify>
    <automated>test -f .planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md && grep -c -e '## Requirement-Text Correction' -e '## Four-tab structure' -e '## Always-visible chrome' -e '## Leaderboard segment toggle' -e '## Friend requests and friendships' -e '## Battles' -e '## Activity feed' -e '## Pull to refresh' -e '## Animations' -e '## Not present in this screen' -e '## How to score FRIENDS-06' .planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md</automated>
    <fails_when>non-zero exit (file absent), or the count printed is less than 11 — one or more required sections is missing.</fails_when>
    <automated>S=$(git status --porcelain mobile/src) || exit 1; test -z "$S" && echo CLEAN || { echo "DIRTY:"; echo "$S"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is anything other than the single word `CLEAN` — source was touched before the baseline was recorded, which destroys the baseline's independence.</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md` exists with all eleven required level-2 sections.
    - The notes state that the capability FRIENDS-01 names does not exist, name `.planning/codebase/CONCERNS.md` as the source of the wording, and document accept, decline and remove as the real behaviors.
    - The notes describe all three leaderboard segments and every branch each can render, including the one-or-fewer-classmates boundary.
    - The notes record that the friends segment substitutes the self-word into the name text and the league segment does not.
    - The notes record the seven-source refresh, the podium remount that follows it, and the order of the two.
    - `git log --oneline -1 -- .planning/phases/03-friendsscreen-decomposition/03-CHARACTERIZATION.md` returns a commit, and no commit touching `mobile/src/screens/FriendsScreen.jsx` is newer than that commit at the time this task ends.
    - Nothing under `mobile/src` is modified by this task.
  </acceptance_criteria>

  <done>A committed, independent, pre-extraction description of FriendsScreen's current behavior exists, and it corrects rather than inherits FRIENDS-01's stale wording.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Create useFriendsScreenState, reduce the screen to one hook call, and prove it with state-transition tests</name>

  <reversibility rating="costly">This hook's flat return shape is the contract all ten remaining plans in this phase consume; changing it later means editing every extracted component and its test. Nothing is published or persisted, so it is costly rather than one-way.</reversibility>

  <files>mobile/src/hooks/useFriendsScreenState.js, mobile/src/screens/FriendsScreen.jsx, mobile/src/__tests__/useFriendsScreenState.test.js</files>

  <read_first>
    - `mobile/src/screens/FriendsScreen.jsx` lines 1-15 and 251-296 — the imports, the four state declarations, the five domain-hook calls with their destructure-renames, the focus effect, and the three local functions. This is the code you are relocating.
    - `mobile/src/screens/FriendsScreen.jsx` lines 332, 455, 519, 561, 593, 645, 687 and 721 — the eight direct navigate call sites, plus 284-289 for the profile one. Each becomes a named handler. Read all nine before naming any of them.
    - `mobile/src/screens/FriendsScreen.jsx` line 584 — the friend-code copy control. Its lazy require inside a try/catch is deliberate; it moves as-is.
    - `mobile/src/hooks/useFriends.js` lines 291-295, `useChallenges.js` line 123, `useLeaderboard.js` line 75, `useFriendsLeaderboard.js` line 82, `useLeague.js` lines 249-258 — the five return objects. You destructure these, so you must know their exact keys.
    - `mobile/src/hooks/useFocusScreenState.js` — the whole file. This is the proven shape: named export, no default, uid read from the auth context inside the hook, handlers that wrap routing so no component ever sees it.
    - `mobile/src/__tests__/useFocusScreenState.test.js` lines 1-100 — the per-file context-mock convention and the awaited `renderHook`/`act` shape.
    - `.planning/phases/03-friendsscreen-decomposition/03-PATTERNS.md` section "`mobile/src/hooks/useFriendsScreenState.js` (hook, CRUD + event-driven)" — the import block, the verbatim destructure lines to copy, and the alert-confirm-then-mutate shape.
    - `.planning/phases/03-friendsscreen-decomposition/03-RESEARCH.md` Pitfalls 1, 2 and 3 — why all five domain hook modules must be mocked at module level, and why the focus effect needs its own per-file mock.
  </read_first>

  <behavior>
    Assertions `mobile/src/__tests__/useFriendsScreenState.test.js` must make. Write them first and watch
    them go red against the not-yet-existing module — that red is the evidence the file is exercising real
    code rather than passing vacuously.

    - Fresh mount with no route params returns `tab` `'Leaderboard'`, `lbMode` `'friends'`, `podiumKey` `0`,
      and `refreshing` `false`.
    - Fresh mount with `route.params.initialTab` set to `'Battles'` returns `tab` `'Battles'`.
    - Fresh mount surfaces every field the containers consume, each present and each holding the fixture
      value its mocked domain hook returned: `friends`, `incomingRequests`, `sentRequests`, `friendCode`,
      `feed`, `incomingBattles`, `completedBattles`, `leaderboard`, `school`, `schoolLoading`,
      `weeklyRanking`, `weekLoading`, `tier`, `leagueMembers`, `leagueMeta`, `uid`, `timeAgo`.
    - With every mocked list empty, `friends`, `incomingRequests`, `sentRequests`, `feed`, `weeklyRanking`,
      `leaderboard`, `leagueMembers`, `incomingBattles` and `completedBattles` are all empty arrays and
      nothing throws.
    - `leagueMeta` falls back to the bronze entry when the mocked tier is an unknown string.
    - `setTab('Friends')` changes `tab` and leaves `lbMode` and `podiumKey` untouched; `setLbMode('school')`
      changes `lbMode` and leaves `tab` untouched.
    - With a five-entry `weeklyRanking` fixture, `top3` holds the first three in their given order and
      `restList` holds the remaining two in their given order.
    - With a three-entry `weeklyRanking` fixture, `top3` holds three and `restList` is empty; with a
      four-entry fixture, `top3` holds three and `restList` holds exactly one.
    - `handleRefresh()` calls each of the seven mocked refresh functions exactly once, increments
      `podiumKey` from 0 to 1, and leaves `refreshing` false once it settles.
    - `confirmRemoveFriend({ id: 'f1', displayName: 'Sam' })` opens a two-button alert; invoking the second
      button's handler calls the mocked remove function with `'f1'`, and invoking the first button's handler
      (if it has one) removes nobody. Spy on the alert rather than rendering it.
    - `confirmRemoveFriend({ uid: 'f2' })` — a record with no `id` — passes `'f2'` to the remove function,
      proving the existing fallback survives.
    - Each named routing handler calls the navigation double's navigate function exactly once with the
      exact destination name and parameter object the pre-extraction screen used. Assert all nine:
      profile, the shared battle-challenge handler, the friend-card battle handler, the rematch handler,
      add-friend, school picker, and the league handler.
    - The hook exposes no field named for the routing object itself.
  </behavior>

  <action>
    Write the test file first, then the hook, then the screen edit.

    **`mobile/src/__tests__/useFriendsScreenState.test.js`.** Flat directory placement alongside
    `useFocusScreenState.test.js`, same naming, block-comment header in this repo's house style naming what
    it guards and citing this plan. Mock, at file scope with paths relative to `mobile/src/__tests__/`:
    `'../context/AuthContext'` exposing `useAuthContext` returning a user whose uid is `'test-uid'`; all
    five domain hook modules — `'../hooks/useFriends'` (exposing both `useFriends` and `timeAgo`),
    `'../hooks/useChallenges'`, `'../hooks/useLeaderboard'`, `'../hooks/useFriendsLeaderboard'`, and
    `'../hooks/useLeague'` (exposing both `useLeague` and `TIER_META`) — each returning a jest.fn whose
    return object has exactly the keys its real counterpart returns; and
    `'@react-native-async-storage/async-storage'` resolved to that package's own shipped jest mock.
    Mocking all five domain hooks is mandatory, not optional: each of them imports the real Firestore
    package at module top level, and this repo's jest transform does not cover it, so merely importing the
    orchestration hook without these mocks crashes the file before any assertion runs (03-RESEARCH.md
    Pitfall 1). Give `TIER_META` a bronze entry plus at least one other tier so the fallback assertion is
    meaningful.

    Also mock the routing package per file, spreading the real module and replacing only its focus-effect
    export with one that runs its callback through a mount-once effect. The reason is mechanical: the
    real focus effect resolves its own context internally and takes no argument, and RNTL's hook renderer
    mounts the hook with no navigator ancestor, so an unmocked call throws before any assertion runs
    (03-RESEARCH.md Pitfalls 2 and 3). Record in the plan SUMMARY that this per-file mock, rather than the
    heavier official test-navigator wrapper, is the chosen trade-off, and that it means the focus behavior
    itself is exercised by the FRIENDS-06 manual pass rather than by this suite.

    Build the navigation double as an object with `navigate`, `goBack`, `dispatch` and an `addListener`
    that returns an unsubscribe function, all jest.fn. Build the route double as a plain object; use
    `undefined` route in the default-tab test. Await every `renderHook` and `act` call.

    **`mobile/src/hooks/useFriendsScreenState.js`.** Named export, no default, matching the sibling
    orchestration hook. Signature `useFriendsScreenState(navigation, route)` — it needs `route` because the
    initial tab derives from a route parameter (D-07), and it reads uid and user from the auth context
    internally rather than taking them as parameters, matching the sibling hook.

    Move from `mobile/src/screens/FriendsScreen.jsx` verbatim: the four state declarations (`tab` seeded
    from the route parameter with the same fallback, `lbMode` seeded to the friends string, `refreshing`,
    `podiumKey`), the five domain-hook calls **copied character-for-character from lines 263-267 including
    every destructure-rename** (D-03, locked — these names are the existing resolution of a real collision
    across four of the five hooks, and inventing new ones is forbidden), the tier-metadata lookup with its
    bronze fallback, the focus effect that re-reads the school leaderboard, the confirm-then-remove
    function, the two derived slices, the profile navigate function, and the refresh orchestration
    including its `Promise.all` over all seven refreshes and its podium-key increment.

    The league hook's own loading field is **not** destructured today and must stay undestructured — the
    shipped screen reads only tier, members and refresh from it, so leaving it alone is the literal
    parity-preserving choice (03-RESEARCH.md Assumption A3).

    Add named handlers for every remaining routing call site so that no component ever receives the routing
    object (D-01). Name them individually rather than behind one generic signature, because the parameter
    shapes genuinely differ: a profile handler taking an entry; one battle-challenge handler taking a
    challenge record and passing its id and originator name — used by **both** the shell banner and the
    Battles-tab pending row, which were read at source level this session and build identical parameters
    from the same array, so one handler is correct here rather than assumed-correct; a separate handler
    taking a friend record and passing that friend's id under the *friend* parameter key; a separate
    rematch handler taking an opponent id and name; an add-friend handler; a school-picker handler; and a
    league handler used by both the tier banner and the promotion-zones footer link. Also move the
    friend-code copy control's body in as a handler, keeping its lazy require inside a try/catch exactly as
    written — that guard is deliberate, and rewriting it to a top-level import would change behavior.

    Return one flat object (D-02). Build it field by field from the destructured domain-hook bindings —
    **do not spread any domain hook's return object wholesale**, because a wholesale spread reintroduces
    exactly the collision D-03 resolves. The object holds: the screen-local state and its setters (`tab`,
    `setTab`, `lbMode`, `setLbMode`, `refreshing`, `podiumKey`); `uid`; the friends data
    (`friends`, `incomingRequests`, `sentRequests`, `friendCode`, `feed`, `acceptRequest`,
    `declineRequest`); the battles data (`incomingBattles`, `completedBattles`); the school leaderboard
    data (`leaderboard`, `school`, `schoolLoading`); the friends leaderboard data (`weeklyRanking`,
    `weekLoading`, `top3`, `restList`); the league data (`tier`, `leagueMembers`, `leagueMeta`); the
    `timeAgo` helper passed straight through so the Activity container never has to import a
    Firestore-importing module itself; and every handler named above plus `handleRefresh` and
    `confirmRemoveFriend`. The seven refresh functions and the remove function stay internal to the hook —
    nothing outside it calls them after this plan. This return shape is the contract plans 03-02 through
    03-10 consume; treat it as frozen and record the final key list in the plan SUMMARY.

    **`mobile/src/screens/FriendsScreen.jsx`.** Delete the relocated state declarations, the five
    domain-hook calls, the tier lookup, the focus effect, the three local functions, and every import they
    were the only consumer of. Call the orchestration hook once and read everything the remaining inline
    JSX needs off that single value. Keep the theme read and the style factory for now — the shell chrome
    and the three still-inline tab branches use them, and plans 03-02 through 03-05 dismantle them. The
    module-local avatar, podium and rank-row functions and the medal and podium constants stay until 03-02
    moves them. Do not otherwise reorder or reformat the JSX: a diff that shows removals plus one hook call
    plus mechanical identifier substitutions is what makes this reviewable.
  </action>

  <verify>
    <automated>npm --prefix mobile install && npm --prefix mobile test -- src/__tests__/useFriendsScreenState.test.js 2>&1 | tail -40</automated>
    <fails_when>non-zero exit; or the output contains `Cannot find module`; or the output contains `Unexpected token 'export'` (a domain hook was left unmocked and its Firestore import reached jest); or the summary line reports any failing test; or the output contains neither `Tests:` nor `Test Suites:` (jest never reached the file).</fails_when>
    <automated>grep -c -e 'useFriends(' -e 'useChallenges(' -e 'useLeaderboard(' -e 'useFriendsLeaderboard(' -e 'useLeague(' mobile/src/hooks/useFriendsScreenState.js</automated>
    <fails_when>the printed count is less than 5 — the orchestration hook is not wrapping all five domain hooks, meaning at least one was absorbed instead.</fails_when>
    <automated>grep -c -e 'schoolLoading' -e 'weekLoading' -e 'refreshSchool' -e 'refreshWeekly' -e 'refreshLeague' -e 'refreshBattles' -e 'incomingBattles' -e 'completedBattles' -e 'leagueMembers' mobile/src/hooks/useFriendsScreenState.js</automated>
    <fails_when>the printed count is less than 9 — one or more of D-03's locked destructure-rename names was dropped or renamed.</fails_when>
    <automated>LOG=$(git log --format=%H --reverse -- .planning/phases/03-friendsscreen-decomposition) || exit 1; FIRST=$(printf '%s\n' "$LOG" | head -1); test -n "$FIRST" || { echo "NO-PHASE-COMMIT"; exit 1; }; D=$(git diff --stat "$FIRST" -- mobile/src/hooks/useFriends.js mobile/src/hooks/useChallenges.js mobile/src/hooks/useLeaderboard.js mobile/src/hooks/useFriendsLeaderboard.js mobile/src/hooks/useLeague.js) || exit 1; test -z "$D" && echo UNCHANGED || { echo "MODIFIED:"; echo "$D"; }</automated>
    <fails_when>non-zero exit (git itself failed, so nothing was measured), or the output is `NO-PHASE-COMMIT`, or the output is anything other than the single word `UNCHANGED` — a domain hook was modified, which this phase forbids. The revision range is pinned to this phase's first commit on purpose: an unpinned comparison reports nothing once a change is committed, so it would pass vacuously.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/hooks/useFriendsScreenState.js` exists, exports `useFriendsScreenState` as a named export, and has no default export.
    - `grep -c 'export function useFriendsScreenState' mobile/src/hooks/useFriendsScreenState.js` prints 1.
    - `grep -c 'useFriendsScreenState(navigation, route)' mobile/src/screens/FriendsScreen.jsx` prints 1 — exactly one orchestration hook call site.
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' mobile/src/screens/FriendsScreen.jsx | grep -cE 'useState\(|useFocusEffect\(|Alert\.alert\(|useAuthContext\(' ` prints 0 — the screen holds no local React state, no focus effect, no alert, and no direct auth-context read.
    - `grep -vE '^[[:space:]]*(//|\*|/\*)' mobile/src/hooks/useFriendsScreenState.js | grep -c '\.\.\.useFriends' ` prints 0 and the same check for the other four domain hooks prints 0 — no domain hook return object is spread wholesale (D-02).
    - `npm --prefix mobile test -- src/__tests__/useFriendsScreenState.test.js` passes with at least 18 assertions covering the behaviors listed above.
    - Comparing the five domain hook files against this phase's first commit reports no change. Pin the revision range explicitly; an unpinned comparison reports nothing once a change is committed and would pass vacuously.
    - The initial red run of the test file, before the hook existed, is recorded verbatim in the plan SUMMARY, along with the frozen return-key list.
  </acceptance_criteria>

  <done>FriendsScreen calls one orchestration hook; that hook owns the screen's former state, effects and handlers, wraps all five untouched domain hooks, and its state transitions and routing handlers are proven by an awaited RNTL test suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract ActivityTab and the shared EmptyState leaf, with render tests</name>

  <files>mobile/src/components/FriendsScreen/EmptyState.jsx, mobile/src/components/FriendsScreen/ActivityTab.jsx, mobile/src/components/FriendsScreen/__tests__/EmptyState.test.jsx, mobile/src/components/FriendsScreen/__tests__/ActivityTab.test.jsx, mobile/src/screens/FriendsScreen.jsx</files>

  <read_first>
    - `mobile/src/screens/FriendsScreen.jsx` lines 733-789 — the Activity tab branch. This JSX is what moves, including the empty-state block at 735-743.
    - `mobile/src/screens/FriendsScreen.jsx` lines 857, 963-985 — the `list`, `feedRow`, `feedIconWrap` and `feedActor` style keys this branch uses. Copy their values byte-for-byte.
    - `mobile/src/screens/FriendsScreen.jsx` lines 608-616, 668-676, 465-473 — the three other empty-state blocks. Confirm for yourself that all four are structurally and stylistically identical before sharing one component; if any differs by even a margin value, split it out instead and say so in the SUMMARY.
    - `mobile/src/components/FocusScreen/FocusDoneScreen.jsx` — the house shape for a container under a screen-scoped component directory: default export, theme read inside the component, local style factory, callback props only.
    - `mobile/src/components/FocusScreen/__tests__/PomodoroCycleDots.test.jsx` — this repo's precedent for a render-tier test on a component with no callback props.
    - `.planning/phases/03-friendsscreen-decomposition/03-UI-SPEC.md` sections "Copywriting Contract", "Color", "Spacing Scale" — the parity lock. Off-grid values stay off-grid.
  </read_first>

  <behavior>
    Assertions the two new test files must make. Both components take zero callback props, so per **D-01**
    both are render tier: assert visible output and explicit presence/absence, never simulate a press.

    `EmptyState.test.jsx`:
    - Renders the icon, heading and body strings it is given.

    `ActivityTab.test.jsx`:
    - With an empty feed, renders the heading `No Activity Yet` and its body copy, and renders zero rows.
    - With a three-item feed, renders all three rows in the order given.
    - A feed item whose originator matches the current uid is attributed with the self-word; one whose
      originator matches a friend record is attributed with that friend's display name; one whose
      originator matches neither is attributed with the generic fallback.
    - A feed item whose type is one of the eight known types renders that type's icon; one whose type is
      unknown renders the fallback icon.
    - A feed item with no text renders the text fallback.
    - A feed item with no timestamp renders the default relative-time string.
  </behavior>

  <action>
    Create `mobile/src/components/FriendsScreen/` and its `__tests__/` subdirectory — both are new.

    **`EmptyState.jsx`.** Default-exported component taking exactly three plain props: the icon glyph, the
    heading string, and the body string. Its JSX is the block that appears four times in the screen today:
    a centred wrapper, the glyph at its current size and bottom margin, the heading in the current heading
    style with its current colour and bottom margin, and the body in the current small style with its
    current muted colour and centred alignment. Copy every numeric and style value byte-for-byte from
    `mobile/src/screens/FriendsScreen.jsx` lines 735-743. Read the theme inside the component. Give the
    file a head comment naming the four call sites it consolidates and stating that it was shared only
    because all four were verified identical — not as a generalisation.

    **`ActivityTab.jsx`.** Default-exported container. Move the Activity branch's JSX across unchanged.
    Keep the per-type icon-and-colour map defined exactly where it is today — inside the per-item mapping
    callback, not hoisted to module scope. Hoisting would be a tidy-up this parity lock does not authorise;
    record the observation in the SUMMARY if you want it fixed later. Keep the attribution chain, the
    timestamp derivation with its numeric and millisecond-method branches, and the text fallback exactly as
    written. Render the shared empty-state leaf for the zero-feed branch instead of the inline block.

    Read the theme inside the component and give the file its own local style factory holding only the keys
    this branch uses — the list wrapper, the feed row, the feed icon wrapper, and the feed actor — each
    copied byte-for-byte from the screen's factory. Import the typography scale and the card-shadow helper
    from the shared styles module, because this branch already uses both today. Do not import the button,
    card, pill or section-label factories: 03-UI-SPEC.md records that this screen has never used them and
    pulling them in would be a visual change.

    Props, all supplied by the orchestration hook's return: the feed array, the friends array, the current
    uid, and the relative-time helper. No callbacks, and nothing from the routing layer.

    **The screen.** Replace the Activity branch's inline JSX with a single render of this container,
    passing the four props from the hook's return value. Remove from the screen's style factory only the
    keys the Activity branch was the sole consumer of — the feed row, feed icon wrapper and feed actor keys.
    Leave the list wrapper key, which the three other branches still use.

    **The tests.** Mock the theme context three levels up from the tests directory so the theme read returns
    a colour-token object; the theme read returns nothing outside a provider and the component would fail
    later and confusingly when a token is read off it (03-RESEARCH.md Pitfall 5). Build props inline per
    test; pass a plain function as the relative-time helper so no Firestore-importing module is imported.
    Await `render()`; query by the visible copy strings 03-UI-SPEC.md pins. Write each file's head comment
    so that it describes the render-only tier **without naming the interaction-helper function the phase's
    tier gate greps for** — Phase 2's Trap 5 is exactly this: a comment describing the absence of a thing
    trips the gate that looks for its presence.
  </action>

  <verify>
    <automated>npm --prefix mobile test -- src/components/FriendsScreen 2>&1 | tail -30</automated>
    <fails_when>non-zero exit; or the summary reports any failing test; or the output contains `No tests found` — the new tests directory was not discovered.</fails_when>
    <automated>npm --prefix mobile run check</automated>
    <fails_when>non-zero exit from either the jest run or the babel parse-check of changed files.</fails_when>
    <automated>grep -c -e 'duoBtn' -e 'elevatedCard' -e 'pillTab' -e 'sectionLabel' mobile/src/components/FriendsScreen/ActivityTab.jsx mobile/src/components/FriendsScreen/EmptyState.jsx</automated>
    <fails_when>any printed per-file count is greater than 0 — a shared style factory this screen has never used was pulled in, which is a visual change.</fails_when>
    <automated>grep -cE 'onPress=|onLongPress=|onChangeText=|onSubmitEditing=|onRequestClose=' mobile/src/components/FriendsScreen/ActivityTab.jsx mobile/src/components/FriendsScreen/EmptyState.jsx</automated>
    <fails_when>any printed per-file count is greater than 0 — these two components are render tier and must declare no interactive handler attribute.</fails_when>
  </verify>

  <acceptance_criteria>
    - `mobile/src/components/FriendsScreen/EmptyState.jsx` and `ActivityTab.jsx` both exist with default exports and their own local style factories.
    - `ActivityTab.jsx` contains the literal copy strings `No Activity Yet` and `Activity`, and `EmptyState` is rendered for the zero-feed branch.
    - `ActivityTab.jsx` contains all eight known activity type keys and the fallback pair, with the same eight colour values 03-UI-SPEC.md lists.
    - `grep -c '@react-navigation' mobile/src/components/FriendsScreen/ActivityTab.jsx` prints 0.
    - Both test files exist, both await `render()`, and neither simulates an interaction — per D-01 these are render tier.
    - `npm --prefix mobile test -- src/components/FriendsScreen` passes.
    - `npm --prefix mobile run check` exits 0.
    - The Activity branch no longer appears as inline JSX in `mobile/src/screens/FriendsScreen.jsx`.
    - `git status --porcelain chromebook src functions shared` prints nothing — mobile-only change set, per the repo's one-app-per-commit rule.
  </acceptance_criteria>

  <done>The Activity tab renders from named components under `components/FriendsScreen/`, driven entirely by the orchestration hook's flat return, and covered by render-tier tests per D-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none newly introduced | This plan relocates already-shipped client-side UI code between files in `mobile/src`. No network call, no persisted-data shape, no auth or session logic, and no new user-input path is added. The five domain hooks that own every Firestore read and write for this screen are not modified by this phase. Structure-only extraction, no behavior change, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | relocation of the confirm-then-remove flow into `useFriendsScreenState` | low | mitigate | ASVS V4: the reciprocal friend-subtree write is enforced server-side by `firestore.rules`, unchanged. Client-side, the destructive confirmation must survive the move — enforced by the two confirm-then-remove assertions in Task 2's `<behavior>` block, including the id-fallback case. |
| T-03-02 | Information disclosure | new test fixtures in `mobile/src/__tests__/useFriendsScreenState.test.js` | low | mitigate | Fixtures use the literal `test-uid` and stub friend records; no real uid, token, friend code or Firestore document is embedded, and all five domain hooks are mocked so no live credential path is reachable from the test. |
| T-03-03 | Tampering | the seven-way refresh orchestration | low | accept | `Promise.all` over seven independent reads has no ordering guarantee between them today either; the extraction preserves the existing shape exactly. No new race is introduced and none is fixed. |
| T-03-SC | Tampering | package-manager installs | low | accept | No install task exists in this phase — `npm --prefix mobile install` only restores the already-locked tree. `03-RESEARCH.md` records the Package Legitimacy Audit as not applicable: zero new external packages, no `[ASSUMED]` or `[SUS]` entries, so no legitimacy checkpoint is warranted. |

ASVS level 1, block on high. No high-severity threat is present in this plan's scope.
</threat_model>

<verification>
- `npm --prefix mobile run check` exits 0 (jest suite plus babel parse-check of changed files).
- `npm --prefix mobile test -- src/__tests__/useFriendsScreenState.test.js` and `npm --prefix mobile test -- src/components/FriendsScreen` both pass.
- All five domain hook files diff clean against this phase's first commit (revision range pinned explicitly, never an unpinned comparison).
- `03-CHARACTERIZATION.md` is committed strictly before the first source commit.
- `git status --porcelain chromebook src functions shared` is empty — mobile-only change set.
</verification>

<success_criteria>
- FRIENDS-01 satisfied: an independent pre-extraction baseline exists and corrects the stale requirement wording rather than inventing code to match it.
- FRIENDS-02 partially satisfied: the screen calls exactly one orchestration hook and holds no local state, effects or alerts; plan 03-05 completes the no-inline-branch-JSX half.
- FRIENDS-03 started: `components/FriendsScreen/` exists with its first container and its first shared leaf.
- FRIENDS-04 satisfied for the orchestration hook: state transitions, the empty/first-mount edge, the `top3`/`restList` ordering property, and the three-versus-four adjacency boundary are covered by automated assertions.
- FRIENDS-05 satisfied for `ActivityTab` and `EmptyState`: both have zero callback props, so both get render-tier tests per D-01.
</success_criteria>

<output>
Create `.planning/phases/03-friendsscreen-decomposition/03-01-SUMMARY.md` when done. Record: the verbatim initial red run of the hook test; the frozen `useFriendsScreenState` return-key list (the contract plans 03-02 through 03-10 consume); the trade-off taken on the focus-effect mock and what it means the automated suite does not cover; whether all four empty-state blocks were confirmed identical; and any behavior discrepancy noticed during the move — as a finding, not a fix.
</output>
