{
  "name": "regents-prep",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run --prefix mobile build:web",
    "preview": "vite preview",
    "build:mobile": "npm run --prefix mobile build:web",
    "copy:images": "cp -r public/images mobile/dist/",
    "deploy:mobile": "npm run build:mobile && npm run copy:images && npx firebase-tools@latest deploy --only hosting:mobile-web",
    "deploy:web": "npm run copy:images && npx firebase-tools@latest deploy --only hosting:regents",
    "deploy:all": "npm run build:mobile && npm run copy:images && npx firebase-tools@latest deploy --only hosting",
    "deploy": "npm run copy:images && npx firebase-tools@latest deploy --only hosting:regents"
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@vitejs/plugin-react": "^4.3.1",
    "firebase-admin": "^12.7.0",
    "vite": "^5.4.2"
  }
}
